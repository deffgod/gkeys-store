# 🔄 G2A Environment Variables Migration Guide

Руководство по миграции с старых имен переменных на новые для G2A интеграции.

---

## 📋 Зачем нужна миграция?

В версии **1.1.0** был полностью переписан G2A Integration Client с улучшенной архитектурой. Вместе с этим были обновлены имена переменных окружения для лучшего соответствия официальной документации G2A и улучшения читаемости.

**Хорошие новости:** Новый клиент **полностью совместим** со старыми именами переменных! Вы можете продолжать использовать старые имена, но рекомендуется мигрировать на новые.

---

## 🔄 Таблица миграции

| Старое имя | Новое имя | Статус |
|------------|-----------|--------|
| `G2A_CLIENT_ID` | `G2A_API_KEY` | ✅ Поддерживается, но выводит warning |
| `G2A_CLIENT_SECRET` | `G2A_API_HASH` | ✅ Поддерживается, но выводит warning |
| `G2A_API_BASE` | `G2A_API_URL` | ✅ Поддерживается, но выводит warning |
| `G2A_EMAIL` | `G2A_EMAIL` | ✅ Не изменилось |
| - | `G2A_ENV` | 🆕 Новая переменная (по умолчанию `sandbox`) |
| - | `G2A_TIMEOUT_MS` | 🆕 Новая переменная (по умолчанию `8000`) |
| - | `G2A_RETRY_MAX` | 🆕 Новая переменная (по умолчанию `2`) |

---

## 📝 Пример миграции

### ❌ Старая конфигурация (все еще работает)

```bash
# Старые имена (работают, но выводят warnings)
G2A_CLIENT_ID=ibHtsEljmCxjOFAn
G2A_CLIENT_SECRET=.HrsPmuOlWjqBMHnQWIgfchUqBTBYcRph
G2A_API_BASE=https://sandboxapi.g2a.com/v1
G2A_EMAIL=Welcome@nalytoo.com
```

### ✅ Новая конфигурация (рекомендуется)

```bash
# Новые имена (рекомендуется)
G2A_API_KEY=ibHtsEljmCxjOFAn
G2A_API_HASH=.HrsPmuOlWjqBMHnQWIgfchUqBTBYcRph
G2A_API_URL=https://sandboxapi.g2a.com/v1
G2A_ENV=sandbox
G2A_EMAIL=Welcome@nalytoo.com

# Опциональные новые настройки
G2A_TIMEOUT_MS=8000
G2A_RETRY_MAX=2
```

---

## 🚀 Пошаговая миграция в Vercel

### Шаг 1: Добавьте новые переменные

1. Откройте **Vercel Dashboard** → Ваш проект → **Settings** → **Environment Variables**
2. Добавьте новые переменные со значениями из старых:

| Новая переменная | Скопируйте значение из |
|------------------|------------------------|
| `G2A_API_KEY` | `G2A_CLIENT_ID` |
| `G2A_API_HASH` | `G2A_CLIENT_SECRET` |
| `G2A_API_URL` | `G2A_API_BASE` |

3. Добавьте новую переменную `G2A_ENV`:
   - Значение: `sandbox` (для тестирования) или `live` (для production)

### Шаг 2: Redeploy проекта

1. После добавления новых переменных сделайте **Redeploy**
2. Проверьте логи - не должно быть warnings о legacy переменных

### Шаг 3: Удалите старые переменные (опционально)

⚠️ **Внимание:** Удаляйте старые переменные только после успешного деплоя с новыми!

1. В **Vercel Dashboard** → **Environment Variables**
2. Удалите (или переименуйте):
   - `G2A_CLIENT_ID`
   - `G2A_CLIENT_SECRET`
   - `G2A_API_BASE`
3. Сделайте еще один **Redeploy** для проверки

---

## 🔍 Как проверить, какие переменные используются?

### Проверка в логах

После деплоя проверьте логи Vercel:

**Если используются старые имена:**
```
[G2A Config] Using legacy variable name G2A_CLIENT_ID. Please migrate to G2A_API_KEY.
[G2A Config] Using legacy variable name G2A_CLIENT_SECRET. Please migrate to G2A_API_HASH.
[G2A Config] Using legacy variable name G2A_API_BASE. Please migrate to G2A_API_URL.
```

**Если используются новые имена:**
```
✅ G2A client initialized successfully
Environment: sandbox
Base URL: https://sandboxapi.g2a.com/v1
```

### Проверка через API

```bash
# Проверьте G2A health endpoint
curl https://your-project.vercel.app/api/g2a/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "g2aEnv": "sandbox",
  "configured": true,
  "baseUrl": "https://sandboxapi.g2a.com/v1"
}
```

---

## 🎯 Приоритет переменных

Если установлены **оба** варианта (старое и новое имя), приоритет имеет **новое имя**:

| Если установлены | Используется |
|------------------|--------------|
| `G2A_API_KEY` + `G2A_CLIENT_ID` | `G2A_API_KEY` ✅ |
| Только `G2A_CLIENT_ID` | `G2A_CLIENT_ID` (с warning) ⚠️ |
| `G2A_API_HASH` + `G2A_CLIENT_SECRET` | `G2A_API_HASH` ✅ |
| Только `G2A_CLIENT_SECRET` | `G2A_CLIENT_SECRET` (с warning) ⚠️ |
| `G2A_API_URL` + `G2A_API_BASE` | `G2A_API_URL` ✅ |
| Только `G2A_API_BASE` | `G2A_API_BASE` (с warning) ⚠️ |

---

## 📊 Сравнение старого и нового подхода

### Старый подход (до версии 1.1.0)

```typescript
// Старый код читал переменные напрямую
const G2A_CLIENT_ID = process.env.G2A_CLIENT_ID || 'default'
const G2A_CLIENT_SECRET = process.env.G2A_CLIENT_SECRET || 'default'
const G2A_API_BASE = process.env.G2A_API_BASE || 'https://sandboxapi.g2a.com/v1'
```

**Проблемы:**
- ❌ Нет валидации переменных
- ❌ Использует defaults, если переменные не установлены
- ❌ Нет warnings о неправильной конфигурации

### Новый подход (версия 1.1.0+)

```typescript
// Новый код использует централизованную загрузку
import { loadG2AEnvVars } from './lib/g2a/config/env.js';

const config = loadG2AEnvVars();
// config.apiKey
// config.apiHash
// config.baseUrl
```

**Преимущества:**
- ✅ Валидация всех обязательных переменных
- ✅ Warnings при использовании legacy имен
- ✅ Нормализация URLs
- ✅ Типобезопасность (TypeScript)
- ✅ Поддержка обратной совместимости

---

## 🆕 Новые возможности

### 1. Переменная `G2A_ENV`

Новая переменная для явного указания окружения:

```bash
G2A_ENV=sandbox  # Для тестирования
G2A_ENV=live     # Для production
```

**Преимущества:**
- Явное указание окружения
- Автоматический выбор правильного `G2A_API_URL` если не указан
- Защита от случайного использования production API в development

### 2. Переменная `G2A_TIMEOUT_MS`

Настройка таймаута запросов к G2A API:

```bash
G2A_TIMEOUT_MS=8000  # 8 секунд (по умолчанию)
G2A_TIMEOUT_MS=10000 # 10 секунд (если API медленно отвечает)
```

### 3. Переменная `G2A_RETRY_MAX`

Максимальное количество повторных попыток при ошибках:

```bash
G2A_RETRY_MAX=2  # 2 повтора (по умолчанию)
G2A_RETRY_MAX=3  # 3 повтора (для нестабильного соединения)
```

---

## ⚠️ Важные замечания

### 1. Не удаляйте старые переменные сразу

```bash
# ❌ НЕ ДЕЛАЙТЕ ТАК:
# 1. Удалить старые переменные
# 2. Добавить новые переменные
# 3. Redeploy

# ✅ ДЕЛАЙТЕ ТАК:
# 1. Добавить новые переменные (оставив старые)
# 2. Redeploy и проверить
# 3. Убедиться, что все работает
# 4. Удалить старые переменные (опционально)
# 5. Redeploy еще раз
```

### 2. Проверяйте окружение

```bash
# Убедитесь, что G2A_ENV соответствует вашему API
G2A_ENV=sandbox  # Должен использоваться с sandboxapi.g2a.com
G2A_ENV=live     # Должен использоваться с api.g2a.com
```

### 3. Обновите документацию команды

Если у вас есть внутренняя документация или README, обновите примеры переменных окружения на новые имена.

---

## 🐛 Troubleshooting

### Проблема: "G2A_API_KEY is required"

**Причина:** Ни `G2A_API_KEY`, ни `G2A_CLIENT_ID` не установлены.

**Решение:**
```bash
# Добавьте хотя бы одну из переменных
G2A_API_KEY=your-api-key
# или
G2A_CLIENT_ID=your-api-key
```

### Проблема: Warnings в логах о legacy переменных

**Причина:** Используются старые имена переменных.

**Решение:** Мигрируйте на новые имена (см. выше).

### Проблема: G2A API возвращает 401 Unauthorized

**Причина:** Неправильный API Key или Hash.

**Решение:**
1. Проверьте значения `G2A_API_KEY` и `G2A_API_HASH` в Vercel Dashboard
2. Убедитесь, что они соответствуют ключам из G2A Seller Panel
3. Проверьте, что `G2A_ENV` соответствует окружению ваших ключей (sandbox/live)

---

## 📚 Дополнительные ресурсы

- **[VERCEL_ENV_SETUP.md](VERCEL_ENV_SETUP.md)** - Полная инструкция по настройке
- **[ENV_VARIABLES_TABLE.md](ENV_VARIABLES_TABLE.md)** - Сводная таблица всех переменных
- **[backend/src/lib/g2a/README.md](backend/src/lib/g2a/README.md)** - Документация нового G2A клиента
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

---

## ✅ Checklist миграции

- [ ] Добавил `G2A_API_KEY` (скопировал из `G2A_CLIENT_ID`)
- [ ] Добавил `G2A_API_HASH` (скопировал из `G2A_CLIENT_SECRET`)
- [ ] Добавил `G2A_API_URL` (скопировал из `G2A_API_BASE`)
- [ ] Добавил `G2A_ENV=sandbox` (или `live`)
- [ ] Сделал Redeploy и проверил логи
- [ ] Нет warnings о legacy переменных
- [ ] G2A health endpoint возвращает `200 OK`
- [ ] Удалил старые переменные (опционально)
- [ ] Обновил внутреннюю документацию

---

**Последнее обновление:** 30 декабря 2024  
**Версия:** 1.1.0
