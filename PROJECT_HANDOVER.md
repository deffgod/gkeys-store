# Project Handover Document

Документ для передачи проекта новой команде разработчиков.

## ✅ Выполненные задачи подготовки к релизу

### 1. Очистка проекта

- ✅ Удалены старые бэкапы БД из `backend/prisma/`
- ✅ Удален `vercel.json.backup`
- ✅ Обновлен `.gitignore` для исключения ненужных файлов

### 2. Переменные окружения

- ✅ Создан `.env.example` в корне проекта (frontend)
- ✅ Создан `backend/.env.example` (backend) с полным списком переменных
- ✅ Все переменные документированы с комментариями
- ✅ Создан скрипт `backend/scripts/setup-env.ts` для автоматической настройки

### 3. База данных и миграции

- ✅ Проверена Prisma схема - все миграции актуальны
- ✅ Создана документация по миграциям: `backend/prisma/MIGRATIONS.md`
- ✅ Создан удобный скрипт `backend/scripts/sync-db.ts` для синхронизации БД

### 4. Документация

- ✅ Создан `QUICK_START.md` - быстрый старт для новой команды
- ✅ Обновлен `README.md` с ссылками на новую документацию
- ✅ Создан `PROJECT_HANDOVER.md` (этот файл)

## 🚀 Быстрый старт для новой команды

См. [QUICK_START.md](QUICK_START.md) для детальных инструкций.

### Основные команды

```bash
# Настройка переменных окружения
cd backend
npm run env:setup

# Синхронизация базы данных
npm run db:setup

# Проверка переменных окружения
npm run check:env
```

## 📁 Структура проекта

```
gkeys2/
├── .env.example              # Frontend env variables template
├── backend/
│   ├── .env.example          # Backend env variables template
│   ├── scripts/
│   │   ├── setup-env.ts      # Setup env files script
│   │   └── sync-db.ts        # Database sync script
│   └── prisma/
│       ├── MIGRATIONS.md     # Migrations documentation
│       └── migrations/       # Database migrations
├── QUICK_START.md            # Quick start guide
└── PROJECT_HANDOVER.md       # This file
```

## 🔧 Новые скрипты

### Environment Setup

```bash
cd backend
npm run env:setup
```

Создает `.env.example` файлы и помогает настроить переменные окружения.

### Database Sync

```bash
cd backend

# Полная синхронизация (generate + migrate + seed)
npm run db:sync sync

# Начальная настройка
npm run db:setup

# Отдельные команды
npm run db:sync generate    # Генерация Prisma Client
npm run db:sync migrate      # Применение миграций
npm run db:sync seed        # Сидирование БД
npm run db:sync studio      # Открыть Prisma Studio
npm run db:sync status      # Проверить статус миграций
```

## 📋 Обязательные переменные окружения

### Frontend (`.env`)

- `VITE_API_BASE_URL` - URL backend API

### Backend (`backend/.env`)

**Обязательные:**
- `DATABASE_URL` - PostgreSQL connection string
- `DIRECT_URL` - Direct database connection (обычно = DATABASE_URL)
- `JWT_SECRET` - JWT secret (минимум 32 символа)
- `JWT_REFRESH_SECRET` - JWT refresh secret (минимум 32 символа, должен отличаться)
- `FRONTEND_URL` - Frontend URL для CORS
- `NODE_ENV` - Окружение (development/production/test)

**Опциональные (но рекомендуемые):**
- `REDIS_URL` - Redis для кэширования
- `EMAIL_*` - Настройки SMTP (можно настроить через админ-панель)

**Опциональные (только если используются):**
- `G2A_*` - G2A интеграция
- `STRIPE_*`, `PAYPAL_*`, `MOLLIE_*` - Payment gateways

Полный список см. в `backend/.env.example` или [docs/ENVIRONMENT_VARIABLES.md](docs/ENVIRONMENT_VARIABLES.md)

## 🗄️ База данных

### Миграции

Все миграции находятся в `backend/prisma/migrations/`. 

**Важно:**
- Не удаляйте существующие миграции
- Создавайте новые миграции для изменений схемы
- Всегда проверяйте миграции перед применением в production

См. [backend/prisma/MIGRATIONS.md](backend/prisma/MIGRATIONS.md) для деталей.

### Текущие миграции

1. `20251205120727_` - Initial schema
2. `20251223180000_add_external_order_id` - External order ID
3. `20260103053940_last_build` - Sessions, FAQs, login history
4. `20260108194553_add_g2a_settings` - G2A settings
5. `20260108201507_add_email_settings` - Email settings
6. `20260109023326_add_g2a_environment` - G2A environment field

## 📚 Документация

- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт
- **[DOCUMENTATION.md](DOCUMENTATION.md)** - Полная документация
- **[docs/ENVIRONMENT_VARIABLES.md](docs/ENVIRONMENT_VARIABLES.md)** - Переменные окружения
- **[backend/prisma/MIGRATIONS.md](backend/prisma/MIGRATIONS.md)** - Миграции
- **[docs/api/](docs/api/)** - API документация

## 🔐 Безопасность

### Важные моменты

1. **Никогда не коммитьте `.env` файлы**
2. **Используйте разные секреты для dev и production**
3. **Генерируйте JWT секреты с помощью `openssl rand -base64 32`**
4. **Регулярно обновляйте зависимости**
5. **Используйте SSL для production БД**

## 🧪 Тестирование

```bash
cd backend

# Все тесты
npm test

# Unit тесты
npm run test:unit

# Integration тесты
npm run test:integration

# С покрытием
npm run test:coverage
```

## 🚢 Деплой

См. [docs/deployment/](docs/deployment/) для детальных гайдов по деплою.

### Основные шаги

1. Настройте переменные окружения в Vercel
2. Убедитесь, что `DIRECT_URL` установлен для production
3. Запустите миграции: `npm run prisma:migrate:deploy`
4. Проверьте подключение к БД

## 📞 Контакты и поддержка

Для вопросов и поддержки обращайтесь к предыдущей команде разработчиков или см. документацию.

## ✅ Чеклист для новой команды

- [ ] Прочитан [QUICK_START.md](QUICK_START.md)
- [ ] Установлены зависимости (frontend и backend)
- [ ] Настроена база данных PostgreSQL
- [ ] Созданы `.env` файлы из `.env.example`
- [ ] Настроены все обязательные переменные окружения
- [ ] Запущена синхронизация БД (`npm run db:setup`)
- [ ] Проверено подключение к БД (`npm run db:check`)
- [ ] Запущено приложение локально
- [ ] Прочитана полная документация [DOCUMENTATION.md](DOCUMENTATION.md)

## 📦 Файлы проекта

### Новые файлы для релиза

- `.env.example` - Frontend environment variables template
- `backend/.env.example` - Backend environment variables template
- `.gitattributes` - Git attributes для правильной обработки файлов
- `.editorconfig` - EditorConfig для единообразия кода
- `CHANGELOG.md` - История изменений
- `RELEASE_CHECKLIST.md` - Чеклист для релиза
- `QUICK_START.md` - Быстрый старт
- `PROJECT_HANDOVER.md` - Документ передачи проекта
- `backend/prisma/MIGRATIONS.md` - Документация по миграциям
- `backend/scripts/setup-env.ts` - Скрипт настройки env
- `backend/scripts/sync-db.ts` - Скрипт синхронизации БД

### Обновленные файлы

- `.gitignore` - Обновлен для исключения бэкапов
- `README.md` - Добавлены ссылки на новую документацию
- `package.json` - Версия обновлена до 1.0.0
- `backend/package.json` - Добавлены новые npm скрипты

### Удаленные файлы

- `vercel.json.backup` - Старый бэкап
- `backend/prisma/backup-*.json` - Старые бэкапы БД (5 файлов)
- `backend/prisma/backup-*.sql` - Старые SQL бэкапы (5 файлов)

---

**Дата подготовки:** 2026-01-09  
**Версия проекта:** 1.0.0  
**Статус:** ✅ Готов к передаче и релизу
