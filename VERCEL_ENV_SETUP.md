# Настройка Environment Variables в Vercel

Пошаговая инструкция по настройке всех необходимых переменных окружения в Vercel.

---

## 📋 Содержание

1. [Метод 1: Vercel Dashboard (Рекомендуется)](#метод-1-vercel-dashboard-рекомендуется)
2. [Метод 2: Vercel CLI](#метод-2-vercel-cli)
3. [Проверка установленных переменных](#проверка-установленных-переменных)
4. [Обновление переменных после деплоя](#обновление-переменных-после-деплоя)
5. [Troubleshooting](#troubleshooting)

---

## Метод 1: Vercel Dashboard (Рекомендуется)

### Шаг 1: Откройте Vercel Dashboard

1. Перейдите на [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. Войдите в свой аккаунт
3. Выберите проект `gkeys2` (или ваш проект)

### Шаг 2: Перейдите в Environment Variables

1. В меню проекта выберите **Settings**
2. В левом сайдбаре выберите **Environment Variables**

### Шаг 3: Добавьте переменные

Для каждой переменной:

1. Нажмите кнопку **Add New**
2. В поле **Key** введите имя переменной (например, `VITE_API_BASE_URL`)
3. В поле **Value** введите значение переменной
4. Выберите окружения:
   - ✅ **Production** - для production деплоев
   - ✅ **Preview** - для preview деплоев (опционально)
   - ✅ **Development** - для локальной разработки (опционально)
5. Нажмите **Save**

### Шаг 4: Добавьте все обязательные переменные

#### Frontend переменные

**VITE_API_BASE_URL**
- Key: `VITE_API_BASE_URL`
- Value: `https://your-project.vercel.app/api` (замените на ваш реальный URL)
- Environments: ✅ Production, ✅ Preview

**Важно**: После первого деплоя обновите значение на реальный Vercel URL проекта.

#### Backend переменные

**DATABASE_URL**
- Key: `DATABASE_URL`
- Value: `postgresql://user:password@host:port/database?sslmode=require`
- Environments: ✅ Production, ✅ Preview

**DIRECT_URL**
- Key: `DIRECT_URL`
- Value: `postgresql://user:password@host:port/database?sslmode=require` (обычно = DATABASE_URL)
- Environments: ✅ Production, ✅ Preview

**JWT_SECRET**
- Key: `JWT_SECRET`
- Value: Сгенерируйте через `openssl rand -base64 32` (минимум 32 символа)
- Environments: ✅ Production, ✅ Preview

**JWT_REFRESH_SECRET**
- Key: `JWT_REFRESH_SECRET`
- Value: Сгенерируйте другой ключ через `openssl rand -base64 32` (минимум 32 символа)
- Environments: ✅ Production, ✅ Preview

**FRONTEND_URL**
- Key: `FRONTEND_URL`
- Value: `https://your-project.vercel.app` (без `/api`)
- Environments: ✅ Production, ✅ Preview

**NODE_ENV**
- Key: `NODE_ENV`
- Value: `production`
- Environments: ✅ Production

#### G2A Integration переменные (опционально)

**G2A_API_URL**
- Key: `G2A_API_URL`
- Value: `https://api.g2a.com/integration-api/v1` (production) или `https://sandboxapi.g2a.com/v1` (sandbox)
- Environments: ✅ Production

**G2A_API_KEY**
- Key: `G2A_API_KEY`
- Value: Ваш G2A API Key из Seller Panel
- Environments: ✅ Production

**G2A_API_HASH**
- Key: `G2A_API_HASH`
- Value: Ваш G2A API Hash из Seller Panel
- Environments: ✅ Production

**G2A_ENV**
- Key: `G2A_ENV`
- Value: `sandbox` (для тестирования) или `live` (для production)
- Environments: ✅ Production

#### Опциональные переменные

**REDIS_URL**
- Key: `REDIS_URL`
- Value: `redis://password@host:port/database`
- Environments: ✅ Production (опционально, но рекомендуется)

### Шаг 5: Передеплойте проект

После добавления всех переменных:

1. Перейдите в раздел **Deployments**
2. Найдите последний deployment
3. Нажмите на три точки (⋯) → **Redeploy**
4. Или сделайте новый commit и push - Vercel автоматически пересоберет проект

**Важно**: Переменные окружения применяются только при новом деплое!

---

## Метод 2: Vercel CLI

### Предварительные требования

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Авторизуйтесь:
   ```bash
   vercel login
   ```

### Добавление переменных

#### Добавить одну переменную

```bash
# Для production
vercel env add VITE_API_BASE_URL production

# Для всех окружений
vercel env add JWT_SECRET production preview development
```

При запросе введите значение переменной.

#### Добавить несколько переменных из файла

Создайте файл `.env.production`:

```bash
VITE_API_BASE_URL=https://your-project.vercel.app/api
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
JWT_SECRET=your-secret-key-here
# ... и т.д.
```

Затем используйте скрипт для добавления всех переменных:

```bash
# Добавить все переменные из файла
while IFS='=' read -r key value; do
  if [[ ! $key =~ ^#.*$ ]] && [[ -n $key ]]; then
    echo "Adding $key..."
    echo "$value" | vercel env add "$key" production
  fi
done < .env.production
```

---

## Проверка установленных переменных

### Через Vercel Dashboard

1. Перейдите в **Settings** → **Environment Variables**
2. Просмотрите список всех установленных переменных
3. Проверьте что все обязательные переменные присутствуют

### Через Vercel CLI

```bash
# Просмотреть все переменные
vercel env ls

# Просмотреть переменные для конкретного окружения
vercel env ls production
```

### Проверка в коде

После деплоя проверьте что переменные доступны:

**Frontend** (в браузере Console):
```javascript
console.log(import.meta.env.VITE_API_BASE_URL);
```

**Backend** (в serverless function logs):
```javascript
console.log(process.env.DATABASE_URL);
```

---

## Обновление переменных после деплоя

### Важно

Переменные окружения применяются только при **новом деплое**. Если вы изменили переменные, нужно передеплоить проект.

### Способы передеплоя

#### 1. Через Dashboard

1. Перейдите в **Deployments**
2. Найдите нужный deployment
3. Нажмите на три точки (⋯) → **Redeploy**

#### 2. Через CLI

```bash
vercel --prod
```

#### 3. Автоматически (при push в Git)

Просто сделайте commit и push - Vercel автоматически пересоберет проект:

```bash
git commit --allow-empty -m "Trigger redeploy for env vars"
git push
```

---

## Troubleshooting

### Проблема: Переменные не применяются

**Причина**: Переменные применяются только при новом деплое.

**Решение**:
1. Убедитесь что переменные сохранены в Vercel Dashboard
2. Передеплойте проект (Redeploy в Dashboard или `vercel --prod`)

### Проблема: "Failed to construct 'URL': Invalid base URL"

**Причина**: `VITE_API_BASE_URL` не установлен или неправильного формата.

**Решение**:
1. Проверьте что `VITE_API_BASE_URL` установлен в Vercel
2. Убедитесь что URL заканчивается на `/api`
3. Убедитесь что URL правильного формата (`https://...` или `http://...`)
4. Передеплойте проект

### Проблема: Переменные видны в Dashboard, но не работают

**Возможные причины**:
1. Переменные установлены для неправильного окружения (например, только для Development, а нужен Production)
2. Проект не был передеплоен после добавления переменных
3. Опечатка в имени переменной

**Решение**:
1. Проверьте что переменные установлены для нужного окружения (Production)
2. Передеплойте проект
3. Проверьте имена переменных на опечатки

### Проблема: Секретные значения видны в логах

**Причина**: Не используйте `console.log()` для секретных переменных в production коде.

**Решение**:
- Удалите все `console.log(process.env.JWT_SECRET)` и подобные из кода
- Используйте логирование только для отладочных целей в development

### Проблема: CORS ошибки

**Причина**: `FRONTEND_URL` не установлен или неправильный.

**Решение**:
1. Проверьте что `FRONTEND_URL` установлен
2. Убедитесь что URL правильного формата (без завершающего слеша)
3. Убедитесь что URL совпадает с реальным URL фронтенда
4. Передеплойте backend

---

## 📝 Чеклист настройки

Используйте этот чеклист для проверки:

### Frontend
- [ ] `VITE_API_BASE_URL` установлен и заканчивается на `/api`
- [ ] URL правильного формата (`https://...`)

### Backend - Database
- [ ] `DATABASE_URL` установлен и правильного формата
- [ ] `DIRECT_URL` установлен (обычно = DATABASE_URL)

### Backend - JWT
- [ ] `JWT_SECRET` установлен (минимум 32 символа)
- [ ] `JWT_REFRESH_SECRET` установлен (минимум 32 символа)
- [ ] `JWT_SECRET` и `JWT_REFRESH_SECRET` разные

### Backend - General
- [ ] `FRONTEND_URL` установлен (без завершающего слеша)
- [ ] `NODE_ENV` установлен в `production`

### G2A Integration (опционально)
- [ ] `G2A_API_URL` установлен
- [ ] `G2A_API_KEY` установлен
- [ ] `G2A_API_HASH` установлен
- [ ] `G2A_ENV` установлен (`sandbox` или `live`)

### После настройки
- [ ] Все переменные установлены для окружения **Production**
- [ ] Проект передеплоен после добавления переменных
- [ ] Проверена работа регистрации и логина
- [ ] Проверена работа админ панели

---

## 📚 Дополнительные ресурсы

- [ENV_SETUP_QUICKSTART.md](ENV_SETUP_QUICKSTART.md) - Минимальный набор переменных
- [ENVIRONMENT_VARIABLES.md](ENVIRONMENT_VARIABLES.md) - Полный справочник
- [Vercel Environment Variables Docs](https://vercel.com/docs/concepts/projects/environment-variables) - Официальная документация Vercel

---

**Последнее обновление**: 2024-12-23
