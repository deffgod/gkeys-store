# Быстрый старт: Настройка Environment Variables

Минимальный набор переменных окружения для работы приложения.

---

## 🚨 Критично для работы приложения

Эти переменные **обязательны** для базовой функциональности (регистрация, логин, админ панель).

### Frontend

#### VITE_API_BASE_URL

**Описание**: Базовый URL API для всех запросов из frontend.

**Формат**: `https://your-project.vercel.app/api`

**Важно**:
- URL должен заканчиваться на `/api`
- Используйте реальный Vercel URL вашего проекта
- После первого деплоя обновите на production URL

**Пример для production**:
```
VITE_API_BASE_URL=https://gkeys2-bj0bay6p4-deffgods-projects.vercel.app/api
```

**Пример для development**:
```
VITE_API_BASE_URL=http://localhost:3001/api
```

---

### Backend

#### DATABASE_URL

**Описание**: Connection string для подключения к PostgreSQL базе данных.

**Формат**: `postgresql://user:password@host:port/database?sslmode=require`

**Пример**:
```
DATABASE_URL=postgresql://postgres:mypassword@db.example.com:5432/gkeys_prod?sslmode=require
```

**Как получить**:
- Используйте managed PostgreSQL (Vercel Postgres, Supabase, Neon, и т.д.)
- Скопируйте connection string из панели управления

---

#### DIRECT_URL

**Описание**: Прямое подключение к базе данных (без connection pooling). Обычно совпадает с `DATABASE_URL`.

**Формат**: `postgresql://user:password@host:port/database?sslmode=require`

**Пример**:
```
DIRECT_URL=postgresql://postgres:mypassword@db.example.com:5432/gkeys_prod?sslmode=require
```

**Примечание**: В большинстве случаев `DIRECT_URL` = `DATABASE_URL`.

---

#### JWT_SECRET

**Описание**: Секретный ключ для подписи JWT access токенов.

**Требования**:
- Минимум 32 символа
- Случайная строка
- Разный для каждого окружения

**Генерация**:
```bash
# Linux/Mac
openssl rand -base64 32

# Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

**Пример**:
```
JWT_SECRET=K8mN2pQ5rS7tU9vW1xY3zA4bC6dE8fG0hI2jK4lM6nO8pQ0rS2tU4vW6xY8z
```

---

#### JWT_REFRESH_SECRET

**Описание**: Секретный ключ для подписи JWT refresh токенов. Должен отличаться от `JWT_SECRET`.

**Требования**: Аналогично `JWT_SECRET` (минимум 32 символа, случайная строка).

**Генерация**: Аналогично `JWT_SECRET` (используйте другую команду для генерации другого значения).

**Пример**:
```
JWT_REFRESH_SECRET=M9nO1pQ3rS5tU7vW9xY1zA3bC5dE7fG9hI1jK3lM5nO7pQ9rS1tU3vW5xY7z
```

---

#### FRONTEND_URL

**Описание**: URL фронтенд приложения. Используется для настройки CORS.

**Формат**: `https://your-project.vercel.app` (без `/api`)

**Важно**:
- Не должен заканчиваться на `/`
- Должен включать протокол (`https://`)

**Пример для production**:
```
FRONTEND_URL=https://gkeys2-bj0bay6p4-deffgods-projects.vercel.app
```

**Пример для development**:
```
FRONTEND_URL=http://localhost:5173
```

---

#### NODE_ENV

**Описание**: Окружение выполнения приложения.

**Значение для production**: `production`

**Пример**:
```
NODE_ENV=production
```

---

## 📋 Минимальный набор для админ панели

Админ панель использует **те же переменные**, что и основное приложение. Дополнительных переменных не требуется.

**Доступ к админ панели** контролируется через:
- Роль пользователя в базе данных (поле `role` в таблице `users`)
- Значение должно быть `ADMIN`

**Как создать admin пользователя**:
1. Зарегистрируйтесь через обычную регистрацию
2. В базе данных обновите роль пользователя:
   ```sql
   UPDATE users SET role = 'ADMIN' WHERE email = 'your-email@example.com';
   ```
3. Войдите в систему - админ панель будет доступна по `/admin`

---

## 🔧 Полный набор для production

Для полной функциональности (включая G2A интеграцию) добавьте:

### G2A Integration Variables

#### G2A_API_URL

**Описание**: Базовый URL G2A Integration API.

**Production**:
```
G2A_API_URL=https://api.g2a.com/integration-api/v1
```

**Sandbox** (для тестирования):
```
G2A_API_URL=https://sandboxapi.g2a.com/v1
```

---

#### G2A_API_KEY

**Описание**: API Key для аутентификации в G2A API.

**Как получить**:
1. Войдите в [G2A Seller Panel](https://www.g2a.com/cooperation/api-integration/)
2. Перейдите в раздел "API Integration"
3. Создайте или скопируйте API Key

**Пример**:
```
G2A_API_KEY=74026b3dc2c6db6a30a73e71cdb138b1e1b5eb7a97ced46689e2d28db1050875
```

---

#### G2A_API_HASH

**Описание**: API Hash для аутентификации в G2A API.

**Как получить**: Аналогично `G2A_API_KEY` - в G2A Seller Panel.

**Пример**:
```
G2A_API_HASH=qdaiciDiyMaTjxMt
```

---

#### G2A_ENV

**Описание**: Окружение G2A API.

**Возможные значения**:
- `sandbox` - для тестирования
- `live` - для production

**Пример**:
```
G2A_ENV=sandbox
```

---

### Опциональные переменные

#### REDIS_URL

**Описание**: Connection string для Redis (для idempotency и метрик).

**Формат**: `redis://password@host:port/database`

**Пример**:
```
REDIS_URL=redis://mypassword@redis.example.com:6379/0
```

**Примечание**: Не обязательна, но рекомендуется для production.

---

## 📝 Пример полной конфигурации для production

```bash
# Frontend
VITE_API_BASE_URL=https://your-project.vercel.app/api

# Backend - Database
DATABASE_URL=postgresql://user:password@db.example.com:5432/gkeys_prod?sslmode=require
DIRECT_URL=postgresql://user:password@db.example.com:5432/gkeys_prod?sslmode=require

# Backend - JWT
JWT_SECRET=your-very-strong-secret-key-minimum-32-characters-long-random-string
JWT_REFRESH_SECRET=different-strong-secret-key-minimum-32-characters-long

# Backend - General
FRONTEND_URL=https://your-project.vercel.app
NODE_ENV=production

# G2A Integration (опционально)
G2A_API_URL=https://api.g2a.com/integration-api/v1
G2A_API_KEY=your-g2a-api-key
G2A_API_HASH=your-g2a-api-hash
G2A_ENV=live

# Redis (опционально, но рекомендуется)
REDIS_URL=redis://password@redis.example.com:6379/0
```

---

## ✅ Проверка конфигурации

### Локальная проверка

```bash
# Проверить backend переменные
cd backend
npm run check:env

# Или вручную
node -e "
  const required = ['DATABASE_URL', 'JWT_SECRET', 'JWT_REFRESH_SECRET', 'FRONTEND_URL'];
  const missing = required.filter(v => !process.env[v]);
  if (missing.length) {
    console.error('❌ Отсутствуют:', missing.join(', '));
    process.exit(1);
  }
  console.log('✅ Все обязательные переменные установлены');
"
```

### Проверка в Vercel

1. Перейдите в Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Убедитесь, что все переменные установлены для окружения **Production**
3. Проверьте значения на правильность (особенно URLs)

---

## 🚀 Быстрая настройка в Vercel

### Через Dashboard

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте каждую переменную:
   - **Key**: Имя переменной (например, `VITE_API_BASE_URL`)
   - **Value**: Значение переменной
   - **Environment**: Выберите **Production** (и **Preview** если нужно)
5. Нажмите **Save**
6. Передеплойте проект (или дождитесь автоматического деплоя)

### Через CLI

```bash
# Добавить переменную для production
vercel env add VITE_API_BASE_URL production
# Введите значение при запросе: https://your-project.vercel.app/api

# Добавить для всех окружений
vercel env add JWT_SECRET production preview development

# Просмотреть все переменные
vercel env ls
```

---

## 🔍 Troubleshooting

### Ошибка: "Failed to construct 'URL': Invalid base URL"

**Причина**: `VITE_API_BASE_URL` не установлен или установлен неправильно.

**Решение**:
1. Проверьте что `VITE_API_BASE_URL` установлен в Vercel Environment Variables
2. Убедитесь что URL заканчивается на `/api`
3. Убедитесь что URL правильного формата (начинается с `http://` или `https://`)
4. Передеплойте проект после добавления переменной

### Ошибка: "Database connection failed"

**Причина**: `DATABASE_URL` не установлен или неправильный.

**Решение**:
1. Проверьте `DATABASE_URL` в Vercel Environment Variables
2. Убедитесь что connection string правильного формата
3. Проверьте что база данных доступна и credentials правильные

### Ошибка: "JWT verification failed"

**Причина**: `JWT_SECRET` или `JWT_REFRESH_SECRET` не установлены или слишком короткие.

**Решение**:
1. Убедитесь что оба секрета установлены
2. Проверьте что каждый секрет минимум 32 символа
3. Убедитесь что `JWT_SECRET` и `JWT_REFRESH_SECRET` разные

---

## 📚 Дополнительные ресурсы

- [ENVIRONMENT_VARIABLES.md](ENVIRONMENT_VARIABLES.md) - Полный справочник всех переменных
- [VERCEL_ENV_SETUP.md](VERCEL_ENV_SETUP.md) - Подробная инструкция настройки в Vercel
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) - Полное руководство по деплою

---

**Последнее обновление**: 2024-12-23
