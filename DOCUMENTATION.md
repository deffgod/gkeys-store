# GKEYS Store - Полная Документация

Полное руководство по установке, настройке, разработке и деплою проекта GKEYS Store.

---

## Содержание

1. [Введение](#введение)
2. [Быстрый старт](#быстрый-старт)
3. [Настройка Environment Variables](#настройка-environment-variables)
4. [Разработка](#разработка)
5. [Тестирование](#тестирование)
6. [Деплой](#деплой)
7. [G2A Интеграция](#g2a-интеграция)
8. [Архитектура](#архитектура)
9. [Troubleshooting](#troubleshooting)

---

## Введение

### Описание проекта

GKEYS Store - современная платформа для продажи игровых ключей с оптимизированным интерфейсом, продвинутыми визуальными эффектами и высокой производительностью.

### Технологический стек

#### Frontend
- **React 19** - Последняя версия React с улучшенной производительностью
- **TypeScript 5.9** - Полная типобезопасность для надежности кода
- **Vite 7** - Молниеносная сборка и HMR
- **React Router 7** - Современная маршрутизация
- **Tailwind CSS 3** - Utility-first CSS фреймворк
- **Framer Motion 12** - Продвинутые анимации
- **GSAP 3** - Профессиональная анимация для сложных эффектов
- **shadcn/ui** - 40+ готовых UI компонентов

#### Backend
- **Express.js** - RESTful API сервер
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - Реляционная база данных
- **TypeScript** - Типизированный backend
- **Redis** (опционально) - Кеширование и idempotency

### Основные возможности

- Полноэкранная hero секция с интерактивным каруселем
- Aurora эффекты и анимации
- Система регистрации и авторизации
- Корзина и избранное (для guest и authenticated пользователей)
- Интеграция с G2A API
- Админ-панель для управления контентом
- Адвансированные фильтры и поиск
- Оптимизация производительности (code splitting, lazy loading)

---

## Быстрый старт

### Предварительные требования

- **Node.js** >= 20.0.0 ([Скачать](https://nodejs.org/))
- **npm** или **yarn** менеджер пакетов
- **PostgreSQL** 15+ ([Скачать](https://www.postgresql.org/download/)) или **Docker** ([Скачать](https://www.docker.com/))

### Установка

#### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd gkeys2
```

#### Шаг 2: Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd backend
npm install
cd ..
```

#### Шаг 3: Настройка базы данных

**Вариант A: Использование Docker (рекомендуется)**

```bash
# Запустить PostgreSQL контейнер
docker run --name gkeys-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=gkeys_store \
  -p 5432:5432 \
  -d postgres:15

# Если контейнер уже существует, просто запустите его
docker start gkeys-postgres
```

**Вариант B: Локальный PostgreSQL**

```bash
createdb gkeys_store
# или через psql:
# psql -U postgres
# CREATE DATABASE gkeys_store;
```

#### Шаг 4: Настройка переменных окружения

**Frontend** (`.env` в корне проекта):

```env
VITE_API_BASE_URL=http://localhost:3001/api
```

**Backend** (`backend/.env`):

```env
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gkeys_store?schema=public
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/gkeys_store?schema=public

# Server
PORT=3001
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production-minimum-32-characters
JWT_REFRESH_SECRET=your-refresh-secret-different-from-jwt-secret-minimum-32-characters

# G2A Integration (опционально для начала)
G2A_API_URL=https://sandboxapi.g2a.com/v1
G2A_API_KEY=your-g2a-api-key
G2A_API_HASH=your-g2a-api-hash
G2A_ENV=sandbox
G2A_EMAIL=Welcome@nalytoo.com

# Redis (опционально)
REDIS_URL=redis://localhost:6379
```

**Важно:** Измените `JWT_SECRET` на безопасную случайную строку в production!

#### Шаг 5: Настройка схемы базы данных

```bash
cd backend

# Генерация Prisma Client
npm run prisma:generate

# Применение миграций
npm run prisma:migrate

# Проверка подключения к БД
npm run db:check

# (Опционально) Заполнение тестовыми данными
npm run prisma:seed
```

#### Шаг 6: Запуск приложения

**Terminal 1 - Frontend:**

```bash
# Из корня проекта
npm run dev
```

Frontend будет доступен на: **http://localhost:5173**

**Terminal 2 - Backend:**

```bash
# Из корня проекта
cd backend
npm run dev
```

Backend API будет доступен на: **http://localhost:3001**

### Тестовые аккаунты

После применения seed данных (`npm run prisma:seed`):

**Администратор:**
- Email: `admin@gkeys.store`
- Password: `admin123`

**Обычный пользователь:**
- Email: `test@example.com`
- Password: `password123`
- Баланс: 100.00 EUR

### Проверка установки

#### Frontend

1. Откройте браузер и перейдите на: **http://localhost:5173**
2. Должна загрузиться главная страница с играми

#### Backend API

```bash
# Health check
curl http://localhost:3001/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"2024-12-05T..."}

# Список игр
curl http://localhost:3001/api/games
```

---

## Настройка Environment Variables

### Frontend Variables

#### VITE_API_BASE_URL

**Описание**: Базовый URL API для frontend. Используется для всех API запросов из React приложения.

**Тип**: `string`

**Обязательно**: ✅ Да

**Формат**: 
- **Должен начинаться с протокола**: `https://` (для production) или `http://` (для localhost)
- **Должен заканчиваться на `/api`**

**Примеры**:

✅ **Правильно**:
```
VITE_API_BASE_URL=https://gkeys2.vercel.app/api
VITE_API_BASE_URL=http://localhost:3001/api
```

❌ **Неправильно**:
```
VITE_API_BASE_URL=gkeys2.vercel.app/api  # отсутствует https://
```

**Где используется**:
- `src/services/api.ts` - базовый URL для всех API запросов
- Все компоненты, делающие API вызовы

**Автоматическое исправление**: 
Если протокол отсутствует, код автоматически добавит `https://` (или `http://` для localhost) с предупреждением в development режиме.

---

### Backend Variables

#### DATABASE_URL

**Описание**: Connection string для подключения к PostgreSQL базе данных. Используется Prisma для подключения к БД.

**Тип**: `string` (PostgreSQL connection string)

**Обязательно**: ✅ Да

**Формат**:
```
postgresql://[user]:[password]@[host]:[port]/[database]?[parameters]
```

**Пример**:
```
DATABASE_URL=postgresql://postgres:password@localhost:5432/gkeys_store?schema=public
```

**Production пример**:
```
DATABASE_URL=postgresql://user:pass@db.example.com:5432/gkeys_prod?sslmode=require
```

**Где используется**:
- `backend/src/config/database.ts` - инициализация Prisma Client
- Prisma migrations
- Все сервисы, работающие с БД

**Безопасность**: 
- Никогда не коммитьте в Git
- Используйте разные значения для development/production
- Включите SSL для production (`?sslmode=require`)

---

#### DIRECT_URL

**Описание**: Прямое подключение к базе данных (без connection pooling). Обычно совпадает с `DATABASE_URL`. Используется Prisma для миграций.

**Тип**: `string` (PostgreSQL connection string)

**Обязательно**: ✅ Да

**Пример**:
```
DIRECT_URL=postgresql://user:password@host:5432/database
```

**Примечание**: В большинстве случаев `DIRECT_URL` = `DATABASE_URL`. Отличается только при использовании connection poolers (например, PgBouncer).

---

#### JWT_SECRET

**Описание**: Секретный ключ для подписи JWT access токенов. Должен быть длинным и случайным.

**Тип**: `string`

**Обязательно**: ✅ Да

**Требования**:
- Минимум 32 символа
- Случайная строка (не используйте простые слова)
- Разный для каждого окружения (development/production)

**Пример генерации**:
```bash
# Linux/Mac
openssl rand -base64 32

# Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

**Пример**:
```
JWT_SECRET=your-very-strong-secret-key-minimum-32-characters-long-random-string
```

**Где используется**:
- `backend/src/utils/jwt.ts` - подпись access токенов
- `backend/src/services/auth.service.ts` - создание JWT при логине

**Безопасность**: 
- Никогда не коммитьте в Git
- Используйте разные значения для каждого окружения
- Регулярно ротируйте в production

---

#### JWT_REFRESH_SECRET

**Описание**: Секретный ключ для подписи JWT refresh токенов. Должен отличаться от `JWT_SECRET`.

**Тип**: `string`

**Обязательно**: ✅ Да

**Требования**:
- Минимум 32 символа
- Должен отличаться от `JWT_SECRET`
- Случайная строка

**Пример**:
```
JWT_REFRESH_SECRET=different-strong-secret-key-for-refresh-tokens-minimum-32-chars
```

---

#### JWT_EXPIRES_IN

**Описание**: Время жизни JWT access токена. Определяет, как долго access токен остается действительным после выдачи.

**Тип**: `string`

**Обязательно**: ❌ Нет

**По умолчанию**: `7d` (7 дней)

**Формат**: Строка в формате времени:
- `s` - секунды (например, `3600s`)
- `m` - минуты (например, `60m`)
- `h` - часы (например, `24h`)
- `d` - дни (например, `7d`)
- `w` - недели (например, `1w`)

**Примеры**:
```
JWT_EXPIRES_IN=24h      # 24 часа
JWT_EXPIRES_IN=7d       # 7 дней (по умолчанию)
JWT_EXPIRES_IN=3600s    # 1 час
```

**Рекомендации**:
- Для production: `24h` или `7d` (баланс между безопасностью и удобством)
- Для development: можно использовать более длительное время (например, `30d`)

---

#### JWT_REFRESH_EXPIRES_IN

**Описание**: Время жизни JWT refresh токена. Определяет, как долго refresh токен остается действительным и может использоваться для получения новых access токенов.

**Тип**: `string`

**Обязательно**: ❌ Нет

**По умолчанию**: `30d` (30 дней)

**Формат**: Аналогично `JWT_EXPIRES_IN`

**Примеры**:
```
JWT_REFRESH_EXPIRES_IN=30d     # 30 дней (по умолчанию)
JWT_REFRESH_EXPIRES_IN=90d     # 90 дней
```

**Важно**: 
- Refresh токен должен иметь более длительное время жизни, чем access токен
- Рекомендуется: `JWT_REFRESH_EXPIRES_IN` должно быть в 3-10 раз больше, чем `JWT_EXPIRES_IN`

---

#### FRONTEND_URL

**Описание**: URL фронтенд приложения. Используется для настройки CORS и генерации ссылок.

**Тип**: `string` (URL)

**Обязательно**: ✅ Да

**Пример**:
```
FRONTEND_URL=https://your-project.vercel.app
```

**Development пример**:
```
FRONTEND_URL=http://localhost:5173
```

**Где используется**:
- `backend/src/index.ts` - настройка CORS middleware
- Email сервисы (если используются) - для генерации ссылок

**Важно**: 
- Не должен заканчиваться на `/`
- Должен включать протокол (`http://` или `https://`)

---

#### NODE_ENV

**Описание**: Окружение выполнения приложения. Влияет на логирование, обработку ошибок и поведение приложения.

**Тип**: `string`

**Обязательно**: ✅ Да

**Возможные значения**:
- `development` - локальная разработка
- `production` - production окружение
- `test` - тестовое окружение

**Пример**:
```
NODE_ENV=production
```

---

#### PORT

**Описание**: Порт, на котором запускается сервер. **Не используется в serverless окружении (Vercel)**.

**Тип**: `number`

**Обязательно**: ❌ Нет

**По умолчанию**: `3001`

**Пример**:
```
PORT=3001
```

**Примечание**: В Vercel serverless functions порт не используется, так как Vercel сам управляет маршрутизацией.

---

#### REDIS_URL / REDIS_GKEYS_REDIS_URL

**Описание**: Connection string для подключения к Redis. Используется для idempotency store (предотвращение дублирования webhook запросов), кеширования и метрик.

**Тип**: `string` (Redis connection string)

**Обязательно**: ❌ Нет (опционально)

**Приоритет**: Если установлены обе переменные, `REDIS_GKEYS_REDIS_URL` имеет приоритет над `REDIS_URL`

**Формат**:
```
redis://[password@]host:port[/database]
```

**Примеры**:
```
REDIS_URL=redis://localhost:6379
REDIS_URL=redis://password@redis.example.com:6379/0
REDIS_GKEYS_REDIS_URL=redis://default:password@redis.example.com:16640
```

**Где используется**:
- `backend/src/config/redis.ts` - инициализация Redis клиента
- `backend/src/services/g2a-webhook.service.ts` - idempotency для webhooks
- `backend/src/services/g2a-metrics.service.ts` - хранение метрик
- `backend/src/services/g2a.service.ts` - синхронизация прогресса и OAuth2 токены
- `backend/src/services/game.service.ts` - кеширование игр
- `backend/src/services/cache.service.ts` - общий кеш сервис

**Примечание**: 
- Если не установлен, idempotency и метрики могут работать некорректно
- Для production рекомендуется использовать Redis для надежности
- Код поддерживает оба имени переменных для гибкости конфигурации

---

### G2A Integration Variables

#### G2A_API_URL

**Описание**: Базовый URL G2A Integration API. Автоматически нормализуется кодом (добавляется `/integration-api/v1` если нужно).

**Тип**: `string` (URL)

**Обязательно**: ✅ Да (если используется G2A)

**Возможные значения**:

**Production**:
```
G2A_API_URL=https://api.g2a.com/integration-api/v1
```
или просто:
```
G2A_API_URL=https://api.g2a.com
```
(код автоматически добавит `/integration-api/v1`)

**Sandbox** (для тестирования):
```
G2A_API_URL=https://sandboxapi.g2a.com/v1
```

**Где используется**:
- `backend/src/config/g2a.ts` - нормализация и валидация URL
- `backend/src/services/g2a.service.ts` - создание Axios клиента

**Документация**: [G2A Integration API Documentation](https://www.g2a.com/integration-api/documentation/)

---

#### G2A_API_KEY

**Описание**: API Key для аутентификации в G2A Integration API. Получается в G2A Seller Panel.

**Тип**: `string`

**Обязательно**: ✅ Да (если используется G2A)

**Как получить**:
1. Войдите в [G2A Seller Panel](https://www.g2a.com/cooperation/api-integration/)
2. Перейдите в раздел "API Integration"
3. Создайте или скопируйте API Key

**Пример**:
```
G2A_API_KEY=74026b3dc2c6db6a30a73e71cdb138b1e1b5eb7a97ced46689e2d28db1050875
```

**Безопасность**: 
- Никогда не коммитьте в Git
- Используйте разные ключи для sandbox и production
- Регулярно ротируйте ключи

---

#### G2A_API_HASH

**Описание**: API Hash для аутентификации в G2A Integration API. Получается в G2A Seller Panel вместе с API Key.

**Тип**: `string`

**Обязательно**: ✅ Да (если используется G2A)

**Как получить**: Аналогично `G2A_API_KEY` - в G2A Seller Panel

**Пример**:
```
G2A_API_HASH=qdaiciDiyMaTjxMt
```

**Важно**: 
- API Hash и API Key должны быть из одной пары (созданы вместе в G2A Seller Panel)
- ⚠️ **Обратная совместимость**: Код также поддерживает переменную `G2A_API_SECRET` как альтернативу `G2A_API_HASH` для обратной совместимости
- Если в Vercel используется `G2A_API_SECRET`, рекомендуется переименовать её в `G2A_API_HASH` для соответствия документации
- При использовании `G2A_API_SECRET` код выведет предупреждение в консоль

---

#### G2A_ENV

**Описание**: Окружение G2A API. Определяет, используется ли sandbox или production API.

**Тип**: `string` (enum)

**Обязательно**: ✅ Да (если используется G2A)

**Возможные значения**:
- `sandbox` - тестовое окружение (рекомендуется для разработки)
- `live` - production окружение

**Пример**:
```
G2A_ENV=sandbox
```

**Примечание**: 
- Используйте `sandbox` для тестирования и разработки
- Переключитесь на `live` только после полного тестирования
- Sandbox не выполняет реальные транзакции

---

#### G2A_EMAIL

**Описание**: Email адрес, связанный с G2A аккаунтом. Используется для генерации API ключа для Export API в production.

**Тип**: `string` (email)

**Обязательно**: ❌ Нет (требуется только для Export API в production)

**Пример**:
```
G2A_EMAIL=Welcome@nalytoo.com
```

**Где используется**:
- `backend/scripts/test-g2a-production.ts` - для тестирования production API
- Генерация API ключа для Export API: `SHA256(G2A_API_KEY + G2A_EMAIL + G2A_API_HASH)`

**Важно**: 
- Требуется реальный email от G2A аккаунта для работы с Export API в production
- Используется только для тестирования и генерации API ключей
- Не используется в основном коде приложения (только в тестовых скриптах)

---

#### G2A_TIMEOUT_MS

**Описание**: Таймаут для запросов к G2A API в миллисекундах.

**Тип**: `number`

**Обязательно**: ❌ Нет

**По умолчанию**: `8000` (8 секунд)

**Пример**:
```
G2A_TIMEOUT_MS=8000
```

**Примечание**: 
- Увеличьте значение, если G2A API медленно отвечает
- Не устанавливайте слишком большое значение (рекомендуется 5-15 секунд)

---

#### G2A_RETRY_MAX

**Описание**: Максимальное количество повторов запросов к G2A API при ошибках.

**Тип**: `number`

**Обязательно**: ❌ Нет

**По умолчанию**: `2`

**Пример**:
```
G2A_RETRY_MAX=2
```

**Примечание**: 
- Используется только для временных ошибок (network errors, 5xx responses)
- Не используется для 4xx ошибок (client errors)

---

### Сводная таблица переменных

| Переменная | Категория | Обязательно | По умолчанию | Приоритет |
|------------|-----------|-------------|--------------|-----------|
| `VITE_API_BASE_URL` | Frontend | ✅ Да | - | 🔴 Критично |
| `DATABASE_URL` | Backend | ✅ Да | - | 🔴 Критично |
| `DIRECT_URL` | Backend | ✅ Да | - | 🔴 Критично |
| `JWT_SECRET` | Backend | ✅ Да | - | 🔴 Критично |
| `JWT_REFRESH_SECRET` | Backend | ✅ Да | - | 🔴 Критично |
| `FRONTEND_URL` | Backend | ✅ Да | - | 🔴 Критично |
| `NODE_ENV` | Backend | ✅ Да | - | 🔴 Критично |
| `PORT` | Backend | ❌ Нет | `3001` | 🟢 Опционально |
| `REDIS_URL` / `REDIS_GKEYS_REDIS_URL` | Backend | ❌ Нет | - | 🟡 Рекомендуется |
| `JWT_EXPIRES_IN` | Backend | ❌ Нет | `7d` | 🟢 Опционально |
| `JWT_REFRESH_EXPIRES_IN` | Backend | ❌ Нет | `30d` | 🟢 Опционально |
| `G2A_API_URL` | G2A | ✅ Да* | `https://api.g2a.com/integration-api/v1` | 🟡 Если используется G2A |
| `G2A_API_KEY` | G2A | ✅ Да* | - | 🟡 Если используется G2A |
| `G2A_API_HASH` | G2A | ✅ Да* | - | 🟡 Если используется G2A |
| `G2A_ENV` | G2A | ✅ Да* | `sandbox` | 🟡 Если используется G2A |
| `G2A_TIMEOUT_MS` | G2A | ❌ Нет | `8000` | 🟢 Опционально |
| `G2A_RETRY_MAX` | G2A | ❌ Нет | `2` | 🟢 Опционально |

\* Обязательно только если используется G2A интеграция

---

### Примеры конфигураций

#### Development (.env)

```bash
# Frontend
VITE_API_BASE_URL=http://localhost:3001/api

# Backend
DATABASE_URL=postgresql://postgres:password@localhost:5432/gkeys_dev
DIRECT_URL=postgresql://postgres:password@localhost:5432/gkeys_dev
JWT_SECRET=dev-secret-key-minimum-32-characters-long
JWT_REFRESH_SECRET=dev-refresh-secret-different-from-jwt-secret
FRONTEND_URL=http://localhost:5173
NODE_ENV=development
PORT=3001

# Redis (опционально)
REDIS_URL=redis://localhost:6379

# G2A (Sandbox для тестирования)
G2A_API_URL=https://sandboxapi.g2a.com/v1
G2A_API_KEY=your-sandbox-api-key
G2A_API_HASH=your-sandbox-api-hash
G2A_ENV=sandbox
```

#### Production (Vercel Environment Variables)

```bash
# Frontend
VITE_API_BASE_URL=https://your-project.vercel.app/api

# Backend
DATABASE_URL=postgresql://user:pass@db.example.com:5432/gkeys_prod?sslmode=require
DIRECT_URL=postgresql://user:pass@db.example.com:5432/gkeys_prod?sslmode=require
JWT_SECRET=production-strong-secret-32-chars-minimum-random
JWT_REFRESH_SECRET=production-different-strong-secret-32-chars
FRONTEND_URL=https://your-project.vercel.app
NODE_ENV=production

# Redis (рекомендуется для production)
REDIS_GKEYS_REDIS_URL=redis://default:password@redis.example.com:16640
# или
REDIS_URL=redis://redis.example.com:6379

# G2A (Production)
G2A_API_URL=https://api.g2a.com/integration-api/v1
G2A_API_KEY=your-production-api-key
G2A_API_HASH=your-production-api-hash
G2A_ENV=live
```

---

### Безопасность

#### ✅ Рекомендации

1. **Никогда не коммитьте** `.env` файлы в Git
2. **Используйте разные значения** для development и production
3. **Регулярно ротируйте** секретные ключи (JWT_SECRET, G2A_API_KEY)
4. **Используйте сильные пароли** для JWT секретов (минимум 32 символа, случайные)
5. **Включите SSL** для production DATABASE_URL (`?sslmode=require`)
6. **Ограничьте доступ** к Environment Variables в Vercel (только нужным членам команды)

#### ❌ Что НЕ делать

- Не используйте простые слова как секреты
- Не используйте одинаковые секреты для разных окружений
- Не делитесь секретами в чатах или email
- Не храните секреты в коде
- Не используйте production секреты в development

---

## Разработка

### Структура проекта

```
gkeys2/
├── src/                          # Frontend source code
│   ├── components/              # React components
│   │   ├── ui/                  # 40+ shadcn/ui components
│   │   │   ├── aurora.tsx      # Aurora effect
│   │   │   ├── click-spark.tsx # Click spark effect
│   │   │   └── game-item-card.tsx # Game card component
│   │   └── home/               # Home page components
│   ├── pages/                   # Application pages
│   ├── services/                # API services
│   ├── hooks/                   # React hooks
│   ├── styles/                  # Styles and design tokens
│   └── types/                   # TypeScript types
├── backend/                     # Backend application
│   ├── src/
│   │   ├── routes/              # API routes
│   │   ├── services/            # Business logic
│   │   ├── controllers/         # Controllers
│   │   ├── middleware/          # Express middleware
│   │   └── types/               # TypeScript types
│   └── prisma/                  # Database schema and migrations
│       ├── schema.prisma        # Prisma schema
│       └── migrations/         # Database migrations
├── docs/                        # Documentation
├── public/                      # Static assets
└── package.json                 # Frontend dependencies
```

### Команды разработки

#### Frontend

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Preview production build
npm run preview

# Линтинг
npm run lint
```

#### Backend

```bash
cd backend

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint

# Форматирование кода
npm run format
```

### Работа с базой данных

#### Prisma Commands

```bash
cd backend

# Генерация Prisma Client
npm run prisma:generate

# Создание и применение миграции
npm run prisma:migrate

# Применение миграций (production)
npm run prisma:migrate:deploy

# Заполнение тестовыми данными
npm run prisma:seed

# Открыть Prisma Studio (GUI для базы данных)
npm run prisma:studio

# Проверка подключения к БД
npm run db:check

# Сброс базы данных (WARNING: удалит все данные!)
npx prisma migrate reset
```

### Добавление компонентов

```bash
# Добавить shadcn/ui компонент
npx shadcn@latest add [component-name]
```

### Code Quality

```bash
# Линтинг frontend
npm run lint

# Линтинг backend
cd backend
npm run lint

# Форматирование backend кода
cd backend
npm run format
```

### Основные страницы

1. **Home Page** (`/`) - Главная страница с hero секцией, best sellers, new releases
2. **Catalog Page** (`/catalog`) - Каталог игр с фильтрами и сортировкой
3. **Cart Page** (`/cart`) - Корзина покупок
4. **Wishlist Page** (`/wishlist`) - Избранное
5. **Support Page** (`/support`) - FAQ и поддержка
6. **Profile Pages** (`/profile/*`) - Профиль пользователя, заказы, баланс
7. **Game Detail** (`/game/:id`) - Детальная страница игры
8. **Admin Panel** (`/admin/*`) - Админ-панель для управления контентом
   - **Dashboard** (`/admin`) - Статистика и обзор системы
   - **Games** (`/admin/games`) - Управление играми (CRUD)
   - **Orders** (`/admin/orders`) - Управление заказами
   - **Transactions** (`/admin/transactions`) - Просмотр транзакций
   - **Users** (`/admin/users`) - Управление пользователями (поиск, баланс, роли, активность)
   - **Payment Management** (`/admin/payments`) - Управление методами оплаты и возвратами
   - **Cart Management** (`/admin/carts`) - Просмотр и управление корзинами пользователей
   - **Wishlist Management** (`/admin/wishlists`) - Просмотр избранного и статистика
   - **FAQ Management** (`/admin/faqs`) - Управление FAQ (CRUD)
   - **G2A Offers** (`/admin/g2a/offers`) - Просмотр G2A предложений
   - **G2A Reservations** (`/admin/g2a/reservations`) - Управление G2A резервациями
   - **G2A Sync** (`/admin/g2a/sync`) - Синхронизация с G2A API
   - **Cache Management** (`/admin/cache`) - Статистика и управление кешем
   - **Blog Posts** (`/admin/blog`) - Управление блог-постами

---

## Тестирование

### Автоматические тесты

#### Интеграционные тесты

```bash
cd backend
npm run test:integration
```

Тесты проверяют:
- ✅ Подключение к Redis
- ✅ Подключение к базе данных
- ✅ Инвалидацию кеша
- ✅ Функции миграции корзины/избранного
- ✅ CRUD операции админ-панели
- ✅ G2A сервис функции
- ✅ Проверку типов TypeScript

#### Тестирование endpoints

```bash
cd backend
npm run test:endpoints
```

### Ручное тестирование

#### 1. Тестирование регистрации и авторизации

**Регистрация нового пользователя:**

```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!",
    "nickname": "TestUser"
  }'
```

**Ожидаемый результат:**
- Статус: 201 Created
- Тело ответа содержит `user`, `token`, `refreshToken`

**Авторизация пользователя:**

```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!"
  }'
```

**Ожидаемый результат:**
- Статус: 200 OK
- Тело ответа содержит `user`, `token`, `refreshToken`

#### 2. Тестирование корзины

**Добавление товара в корзину (guest):**

```bash
curl -X POST http://localhost:3001/api/cart \
  -H "Content-Type: application/json" \
  -H "Cookie: sessionId=YOUR_SESSION_ID" \
  -d '{
    "gameId": "GAME_ID",
    "quantity": 1
  }'
```

**Миграция корзины после логина:**

```bash
curl -X POST http://localhost:3001/api/cart/migrate \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Cookie: sessionId=YOUR_SESSION_ID"
```

**Ожидаемый результат:**
- Статус: 200 OK
- Сообщение: "Cart migrated successfully"
- Товары из session cart должны появиться в user cart

#### 3. Тестирование избранного

**Добавление в избранное (guest):**

```bash
curl -X POST http://localhost:3001/api/wishlist \
  -H "Content-Type: application/json" \
  -H "Cookie: sessionId=YOUR_SESSION_ID" \
  -d '{
    "gameId": "GAME_ID"
  }'
```

**Миграция избранного после логина:**

```bash
curl -X POST http://localhost:3001/api/wishlist/migrate \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Cookie: sessionId=YOUR_SESSION_ID"
```

#### 4. Тестирование админ-панели

**Создание игры с G2A полями:**

```bash
curl -X POST http://localhost:3001/api/admin/games \
  -H "Authorization: Bearer ADMIN_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Test Game",
    "slug": "test-game",
    "description": "Test description",
    "price": 10.99,
    "imageUrl": "https://example.com/image.jpg",
    "platform": "Steam",
    "genre": "Action",
    "tags": ["test"],
    "g2aProductId": "test-g2a-id",
    "g2aStock": true
  }'
```

**Ожидаемый результат:**
- Статус: 200 OK
- Игра создана с G2A полями
- Кеш Redis инвалидирован

**Обновление игры с G2A полями:**

```bash
curl -X PUT http://localhost:3001/api/admin/games/GAME_ID \
  -H "Authorization: Bearer ADMIN_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "g2aProductId": "updated-g2a-id",
    "g2aStock": false,
    "g2aLastSync": "2024-01-01T00:00:00.000Z"
  }'
```

#### 5. Тестирование G2A синхронизации

**Проверка статуса G2A:**

```bash
curl -X GET http://localhost:3001/api/admin/g2a/status \
  -H "Authorization: Bearer ADMIN_ACCESS_TOKEN"
```

**Запуск синхронизации G2A:**

```bash
curl -X POST http://localhost:3001/api/admin/g2a/sync \
  -H "Authorization: Bearer ADMIN_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "fullSync": true,
    "includeRelationships": true
  }'
```

**Проверка прогресса синхронизации:**

```bash
curl -X GET http://localhost:3001/api/admin/g2a/sync-progress \
  -H "Authorization: Bearer ADMIN_ACCESS_TOKEN"
```

#### 6. Тестирование кеша Redis

**Проверка кеширования OAuth2 токенов:**

1. Запустите G2A синхронизацию
2. Проверьте Redis:
   ```bash
   redis-cli
   > GET g2a:oauth2:token
   > GET g2a:oauth2:token:expiry
   ```

**Проверка инвалидации кеша:**

1. Создайте/обновите игру через админ-панель
2. Проверьте Redis:
   ```bash
   redis-cli
   > KEYS home:*
   > KEYS game:*
   > KEYS catalog:*
   ```

### Тестовые сценарии

#### Сценарий 1: Полный цикл пользователя

1. ✅ Регистрация нового пользователя
2. ✅ Добавление товаров в корзину как guest
3. ✅ Добавление товаров в избранное как guest
4. ✅ Авторизация
5. ✅ Проверка миграции корзины и избранного
6. ✅ Проверка, что товары доступны в user cart/wishlist

#### Сценарий 2: Админ редактирование

1. ✅ Создание игры через админ-панель
2. ✅ Добавление G2A полей
3. ✅ Обновление G2A полей
4. ✅ Проверка инвалидации кеша
5. ✅ Проверка, что изменения отображаются на фронтенде

#### Сценарий 3: G2A синхронизация

1. ✅ Запуск G2A синхронизации
2. ✅ Проверка прогресса синхронизации
3. ✅ Проверка обновления продуктов в базе данных
4. ✅ Проверка инвалидации кеша
5. ✅ Проверка OAuth2 токенов в Redis

### Метрики успешного тестирования

После успешного тестирования должны быть выполнены:

- ✅ Все интеграционные тесты проходят
- ✅ Регистрация и авторизация работают
- ✅ Корзина и избранное работают для guest и authenticated пользователей
- ✅ Миграция корзины/избранного происходит автоматически при логине
- ✅ Админ-панель позволяет редактировать G2A поля
- ✅ Кеш Redis инвалидируется при изменениях
- ✅ G2A синхронизация работает и отслеживается
- ✅ Ошибки обрабатываются корректно

---

## Деплой

### Предварительные требования

Перед началом деплоя убедитесь, что у вас есть:

- ✅ **Node.js 20+** установлен
- ✅ **Git репозиторий** с кодом проекта
- ✅ **Vercel аккаунт** (бесплатный: https://vercel.com/signup)
- ✅ **PostgreSQL база данных** (для production)
- ✅ **G2A API credentials** (API Key и API Hash)
- ✅ **Redis** (опционально, но рекомендуется)

### Подготовка к деплою

#### 1. Git подготовка

Убедитесь, что все изменения закоммичены:

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "feat: Prepare for production deployment"

# Отправить в репозиторий
git push origin main
```

#### 2. Локальная проверка сборки

Проверьте, что проект собирается без ошибок:

```bash
# Собрать frontend и backend
npm run build:all

# Проверить, что dist/ и backend/dist/ созданы
ls -la dist/
ls -la backend/dist/
```

#### 3. Подготовка Environment Variables

Подготовьте список всех необходимых переменных окружения. См. раздел [Настройка Environment Variables](#настройка-environment-variables) выше.

**Минимально необходимые для первого деплоя:**

- `DATABASE_URL` - строка подключения к PostgreSQL
- `DIRECT_URL` - прямая строка подключения к БД
- `JWT_SECRET` - секретный ключ для JWT (32+ символов)
- `JWT_REFRESH_SECRET` - секретный ключ для refresh токенов
- `FRONTEND_URL` - URL фронтенда для CORS
- `NODE_ENV` - `production`
- `G2A_API_KEY` - G2A API ключ (если используется G2A)
- `G2A_API_HASH` - G2A API хеш (если используется G2A)
- `G2A_API_URL` - URL G2A API (если используется G2A)
- `VITE_API_BASE_URL` - будет установлен после первого деплоя

---

### Деплой на Vercel

#### Метод 1: Vercel Dashboard (Рекомендуется)

##### Шаг 1: Импорт проекта

1. Перейдите на https://vercel.com/new
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"Import Git Repository"**
4. Выберите ваш репозиторий `gkeys2`
5. Нажмите **"Import"**

##### Шаг 2: Настройка проекта

В форме настройки проекта укажите:

- **Framework Preset**: `Vite`
- **Root Directory**: `.` (оставьте пустым или укажите точку)
- **Build Command**: `npm run vercel-build`
- **Output Directory**: `dist`
- **Install Command**: `npm install && cd backend && npm install`

**Важно**: Используйте именно `npm run vercel-build`, который собирает и frontend, и backend.

##### Шаг 3: Environment Variables

Перед деплоем добавьте все необходимые переменные окружения:

1. Нажмите **"Environment Variables"**
2. Добавьте переменные для **Production**, **Preview** и **Development**

**Frontend:**
```
VITE_API_BASE_URL = https://your-project.vercel.app/api
```
*(Используйте URL, который вы получите после деплоя. Можно обновить позже.)*

**Backend (обязательные):**
```
DATABASE_URL = postgresql://user:password@host:5432/database
DIRECT_URL = postgresql://user:password@host:5432/database
JWT_SECRET = your-strong-secret-key-minimum-32-characters
JWT_REFRESH_SECRET = different-strong-secret-key-minimum-32-characters
FRONTEND_URL = https://your-project.vercel.app
NODE_ENV = production
G2A_API_URL = https://api.g2a.com/integration-api/v1
G2A_API_KEY = your-g2a-api-key
G2A_API_HASH = your-g2a-api-hash
G2A_ENV = sandbox
```

**Опциональные:**
```
REDIS_URL = redis://host:6379
PORT = 3001
G2A_TIMEOUT_MS = 8000
G2A_RETRY_MAX = 2
```

3. Нажмите **"Save"** для каждой переменной

##### Шаг 4: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (~3-5 минут)
3. Скопируйте URL деплоя (например: `https://gkeys2-xxx.vercel.app`)

##### Шаг 5: Обновление VITE_API_BASE_URL

После первого деплоя:

1. Скопируйте URL деплоя
2. Перейдите в **Settings → Environment Variables**
3. Обновите `VITE_API_BASE_URL` на: `https://your-actual-url.vercel.app/api`
4. Нажмите **"Redeploy"** или подождите автоматического передеплоя

#### Метод 2: Vercel CLI

##### Установка и авторизация

```bash
# Установить Vercel CLI глобально
npm i -g vercel

# Войти в аккаунт
vercel login
```

##### Первый деплой

```bash
# Из корня проекта
cd /path/to/gkeys2

# Запустить деплой
vercel

# Следовать подсказкам:
# - Set up and deploy? → Yes
# - Which scope? → выберите ваш аккаунт
# - Link to existing project? → No (для первого деплоя)
# - Project name? → gkeys2
# - Directory? → ./
# - Override settings? → No
```

##### Установка Environment Variables через CLI

```bash
# Добавить переменную
vercel env add DATABASE_URL production
# Введите значение при запросе

# Добавить для всех окружений
vercel env add JWT_SECRET production preview development

# Просмотреть все переменные
vercel env ls
```

##### Production деплой

```bash
# Деплой в production
vercel --prod
```

---

### После деплоя

#### 1. Миграции базы данных

**Миграции применяются автоматически** во время процесса сборки на Vercel. Команда `vercel-build` включает `prisma migrate deploy`, которая применяет все непримененные миграции к production базе данных.

**Важно**: 
- Убедитесь, что `DATABASE_URL` и `DIRECT_URL` правильно настроены в Vercel Environment Variables
- Миграции применяются только для новых миграций (безопасно для production)
- Если нужно применить миграции вручную (например, при проблемах), используйте:

```bash
cd backend
DATABASE_URL="your-production-database-url" npm run prisma:migrate:deploy
```

#### 2. Проверка деплоя

##### Frontend

1. Откройте URL деплоя в браузере
2. Проверьте, что главная страница загружается
3. Откройте DevTools → Console, убедитесь, что нет ошибок
4. Проверьте Network tab - API запросы должны идти на `/api/*`

##### Backend API

Проверьте health endpoint:

```bash
curl https://your-project.vercel.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-12-23T...",
  "checks": {
    "database": "ok",
    "redis": "ok",
    "g2a": "ok"
  }
}
```

#### 3. Тестирование endpoints

**Регистрация пользователя:**

```bash
curl -X POST https://your-project.vercel.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test1234",
    "nickname": "TestUser"
  }'
```

**Получение списка игр:**

```bash
curl https://your-project.vercel.app/api/games
```

#### 4. Обновление VITE_API_BASE_URL

Если вы использовали placeholder URL при первом деплое:

1. Скопируйте реальный URL деплоя
2. Перейдите в Vercel Dashboard → Project → Settings → Environment Variables
3. Обновите `VITE_API_BASE_URL` на: `https://your-actual-url.vercel.app/api`
4. Нажмите **"Redeploy"** или подождите автоматического передеплоя

---

### Архитектура деплоя

#### Монолитный деплой на Vercel

Проект использует **монолитную архитектуру** - frontend и backend деплоятся в одном Vercel проекте:

```
Vercel Deployment
├── Frontend (Static Files)
│   ├── dist/index.html
│   ├── dist/assets/*.js
│   └── dist/assets/*.css
│
└── Backend (Serverless Function)
    └── api/index.ts → Express App
        ├── /api/auth/*
        ├── /api/games/*
        ├── /api/orders/*
        └── ... (все API routes)
```

#### Поток запросов

1. Пользователь делает запрос к `/games` или `/api/games`
2. Vercel Router проверяет путь:
   - Если `/api/*` → маршрутизирует к serverless function `api/index.ts`
   - Если не `/api/*` → возвращает `dist/index.html` (SPA routing)
3. Serverless function обрабатывает запрос через Express app
4. Express routes обрабатывают запрос и возвращают ответ

#### Serverless Functions

- **Файл**: `api/index.ts`
- **Runtime**: Node.js 20.x (автоматически)
- **Триггер**: Все запросы к `/api/*`
- **Время выполнения**: До 60 секунд (Hobby), до 5 минут (Pro)
- **Cold start**: ~1-2 секунды при первом запросе

#### Rewrites Configuration

В `vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api|assets|_next).*)",
      "destination": "/index.html"
    }
  ]
}
```

Это означает:
- `/api/*` → serverless function `api/index.ts`
- Все остальные пути → `index.html` (SPA routing)

---

### Чеклист успешного деплоя

После деплоя проверьте:

- [ ] Frontend загружается: `https://your-project.vercel.app`
- [ ] Health check работает: `https://your-project.vercel.app/api/health`
- [ ] Регистрация работает: `POST /api/auth/register`
- [ ] Логин работает: `POST /api/auth/login`
- [ ] Игры загружаются: `GET /api/games`
- [ ] Нет CORS ошибок в консоли браузера
- [ ] Миграции базы данных применены
- [ ] Environment variables установлены правильно
- [ ] G2A интеграция работает (проверьте `/api/health` - должно быть `g2a: ok`)

---

## G2A Интеграция

### Настройка G2A API

#### Получение credentials

1. Войдите в [G2A Seller Panel](https://www.g2a.com/cooperation/api-integration/)
2. Перейдите в раздел **API Integration**
3. Создайте или скопируйте:
   - **API Key** → это будет `G2A_API_KEY`
   - **API Hash** → это будет `G2A_API_HASH`

#### Настройка переменных окружения

**Для Sandbox (тестирование):**
```env
G2A_API_URL=https://sandboxapi.g2a.com/v1
G2A_API_KEY=your-sandbox-api-key
G2A_API_HASH=your-sandbox-api-hash
G2A_ENV=sandbox
```

**Для Production:**
```env
G2A_API_URL=https://api.g2a.com/integration-api/v1
G2A_API_KEY=your-production-api-key
G2A_API_HASH=your-production-api-hash
G2A_ENV=live
```

### Sandbox окружение

#### Что такое G2A Sandbox?

G2A Sandbox - это тестовое окружение G2A API, которое позволяет тестировать интеграцию без реальных транзакций. Sandbox использует тестовые данные и не выполняет реальные покупки.

#### Pre-defined Test Credentials

G2A предоставляет предопределенные тестовые credentials для быстрого старта:

- **Test Client Id (G2A_API_HASH)**: `qdaiciDiyMaTjxMt`
- **Test Api Key (G2A_API_KEY)**: `74026b3dc2c6db6a30a73e71cdb138b1e1b5eb7a97ced46689e2d28db1050875`

Эти credentials можно использовать сразу для тестирования.

#### Разница между Sandbox и Production

**Sandbox API:**
- **URL**: `https://sandboxapi.g2a.com/v1`
- **Аутентификация**: Упрощенная (Authorization header: `"{hash}, {key}"`)
- **Данные**: Тестовые продукты и транзакции
- **Транзакции**: Не выполняются реально, только симуляция
- **Использование**: Для разработки и тестирования

**Production API:**
- **URL**: `https://api.g2a.com/integration-api/v1`
- **Аутентификация**: Hash-based с timestamp (Import API) или Authorization header (Export API)
- **Данные**: Реальные продукты и транзакции
- **Транзакции**: Выполняются реально
- **Использование**: Для production окружения

#### Проверка настройки Sandbox

```bash
# В backend директории
cd backend
npm run test:g2a
# или
node scripts/test-g2a.ts
```

После запуска приложения проверьте логи:
- ✅ **Успешно**: `[G2A] Creating G2A API client` с `isSandbox: true`
- ✅ **Успешно**: `baseURL: 'https://sandboxapi.g2a.com/v1'`
- ❌ **Ошибка**: `baseURL: 'https://api.g2a.com/integration-api/v1'` (production вместо sandbox)

### Production окружение

#### Переключение на Production

**⚠️ ВНИМАНИЕ**: Переключайтесь на production только после полного тестирования в sandbox!

1. Получите production credentials из G2A Seller Panel
2. Обновите переменные окружения:
   - `G2A_API_URL=https://api.g2a.com/integration-api/v1`
   - `G2A_API_KEY=your-production-api-key`
   - `G2A_API_HASH=your-production-api-hash`
   - `G2A_ENV=live`
3. Перезапустите приложение
4. Проверьте логи - должно быть `isSandbox: false`

#### Тестирование Production API

Скрипт для тестирования production API:

```bash
cd backend
G2A_API_KEY="your-client-id" \
G2A_API_HASH="your-client-secret" \
G2A_API_URL="https://api.g2a.com" \
G2A_EMAIL="Welcome@nalytoo.com" \
npx tsx scripts/test-g2a-production.ts
```

**Важно**: Для Export API требуется реальный email от G2A аккаунта.

#### Возможные проблемы Production API

**Проблема: "Wrong Authorization header"**

**Возможные причины**:
1. Email неправильный - нужен реальный email от G2A аккаунта
2. IP адрес не добавлен в whitelist G2A
3. Аккаунт не активирован для production API

**Решение**:
- Проверьте email в G2A Seller Panel
- Добавьте IP адрес в whitelist (G2A Seller Panel → API Integration → IP Whitelist)
- Убедитесь, что аккаунт верифицирован для production

**Проблема: OAuth2 Token 404**

**Возможные причины**:
1. Неправильный путь endpoint
2. Неправильная аутентификация для получения токена
3. Import API требует отдельной активации

**Решение**:
- Проверьте путь: `GET /integration-api/v1/token`
- Убедитесь, что используется hash-based аутентификация для получения токена
- Свяжитесь с G2A Support если проблемы сохраняются

### Методы аутентификации G2A

#### 1. OAuth2 Token Authentication (Import API)

**Метод**: OAuth2 token-based authentication для Import API операций

**Token Endpoint**: `GET /integration-api/v1/token`

**Использование**:
- Token получается via `GET /token` используя hash-based auth
- Token кешируется в Redis с TTL matching `expires_in` (3600 секунд)
- Token автоматически обновляется если истекает в течение 5 минут
- Token используется в `Authorization: Bearer {access_token}` header для Import API запросов

**Когда использовать**: Все Import API endpoints (offers, jobs, bestsellers, reservations, price simulations)

#### 2. Hash-based Authentication (Export API - Production)

**Метод**: Hash-based authentication с SHA-256 hash generation

**Headers Required**:
- `X-API-HASH`: G2A_API_HASH (из environment variable)
- `X-API-KEY`: G2A_API_KEY (из environment variable)
- `X-G2A-Timestamp`: Unix timestamp в секундах (текущее время)
- `X-G2A-Hash`: SHA-256 hash of (G2A_API_HASH + G2A_API_KEY + timestamp)

**Hash Generation**:
```
timestamp = Math.floor(Date.now() / 1000).toString()
hash = SHA256(G2A_API_HASH + G2A_API_KEY + timestamp)
```

**Когда использовать**: Все Export API endpoints в production (products, orders, order details, order keys, payment)

#### 3. Authorization Header (Export API - Sandbox)

**Метод**: Упрощенная аутентификация для sandbox

**Headers Required**:
- `Authorization: "{G2A_API_HASH}, {G2A_API_KEY}"`

**Когда использовать**: Все Export API endpoints в sandbox

#### 4. Authorization Header (Export API - Developers API)

**Метод**: Authorization header с sha256 hash для Export API (Developers API)

**Headers Required**:
- `Authorization: "{ClientId}, {ApiKey}"`
- где `ApiKey = sha256(ClientId + Email + ClientSecret)`

**Когда использовать**: Export API endpoints согласно G2A Developers API документации

### Важные замечания

#### ⚠️ Не используйте production API для тестирования!

- Всегда используйте sandbox для разработки и тестирования
- Production API выполняет реальные транзакции
- Используйте production только после полного тестирования

#### ⚠️ Не коммитьте credentials в Git!

- Никогда не добавляйте `.env` файлы в Git
- Используйте `.gitignore` для исключения `.env` файлов
- Храните credentials только в переменных окружения

#### ⚠️ Разные credentials для sandbox и production

- Sandbox и production используют разные credentials
- Не используйте sandbox credentials в production
- Не используйте production credentials в sandbox

---

## Архитектура

### Frontend структура

```
src/
├── components/              # React components
│   ├── ui/                 # 40+ shadcn/ui components
│   └── home/              # Home page components
├── pages/                  # Application pages
├── services/               # API services
│   ├── api.ts             # Main API client
│   ├── authApi.ts         # Authentication API
│   ├── cartApi.ts         # Cart API
│   └── wishlistApi.ts     # Wishlist API
├── context/                # React contexts
│   ├── AuthContext.tsx    # Authentication context
│   ├── CartContext.tsx    # Cart context
│   └── WishlistContext.tsx # Wishlist context
├── hooks/                  # Custom React hooks
├── styles/                 # Styles and design tokens
└── types/                  # TypeScript types
```

### Backend структура

```
backend/src/
├── config/                 # Configuration
│   ├── database.ts        # Prisma client
│   ├── redis.ts           # Redis client
│   └── g2a.ts             # G2A configuration
├── routes/                 # API routes
│   ├── auth.routes.ts     # Authentication routes
│   ├── games.routes.ts    # Games routes
│   ├── cart.routes.ts     # Cart routes
│   ├── wishlist.routes.ts # Wishlist routes
│   └── admin.routes.ts    # Admin routes
├── services/               # Business logic
│   ├── auth.service.ts    # Authentication service
│   ├── game.service.ts    # Game service
│   ├── cart.service.ts    # Cart service
│   ├── wishlist.service.ts # Wishlist service
│   ├── g2a.service.ts     # G2A integration service
│   ├── cache.service.ts   # Cache service
│   └── admin.service.ts   # Admin service
├── controllers/            # Controllers
│   ├── auth.controller.ts
│   ├── game.controller.ts
│   └── admin.controller.ts
├── middleware/             # Express middleware
│   ├── auth.ts            # Authentication middleware
│   ├── session.middleware.ts # Session middleware
│   └── errorHandler.ts    # Error handling
├── types/                  # TypeScript types
└── index.ts                # Entry point
```

### Интеграция Frontend-Backend

#### API Client

Frontend использует `src/services/api.ts`, который автоматически:
- Читает `VITE_API_BASE_URL` из environment
- Добавляет `/api` prefix ко всем запросам
- Обрабатывает JWT токены из localStorage
- Устанавливает `Authorization: Bearer <token>` header

**Пример использования**:
```typescript
import apiClient from './services/api';

// GET request
const games = await apiClient.get('/games', { params: { page: 1 } });

// POST request with auth
const profile = await apiClient.get('/user/profile'); // Auto-adds token
```

#### Backend API Structure

Все API routes имеют prefix `/api`:
- `/api/auth/*` - Authentication
- `/api/games/*` - Games catalog
- `/api/user/*` - User profile (protected)
- `/api/orders/*` - Orders (protected)
- `/api/cart/*` - Shopping cart (protected)
- `/api/wishlist/*` - Wishlist (protected)
- `/api/admin/*` - Admin panel (admin only)
  - `/api/admin/dashboard` - Dashboard statistics
  - `/api/admin/games` - Games CRUD
  - `/api/admin/orders` - Orders management
  - `/api/admin/transactions` - Transactions viewing
  - `/api/admin/users` - User management (search, balance, roles, activity)
  - `/api/admin/payments/*` - Payment methods and transaction management
  - `/api/admin/carts/*` - Cart management (view, update, clear)
  - `/api/admin/wishlists/*` - Wishlist management (view, statistics)
  - `/api/admin/faqs/*` - FAQ CRUD operations
  - `/api/admin/g2a/offers/*` - G2A offers viewing
  - `/api/admin/g2a/reservations/*` - G2A reservations management
  - `/api/admin/g2a/sync` - G2A synchronization
  - `/api/admin/cache/*` - Cache statistics and invalidation
  - `/api/admin/blog/*` - Blog posts CRUD
- `/api/g2a/*` - G2A webhooks (public)

#### Authentication Flow

1. **Registration/Login**:
   ```typescript
   import { authApi } from './services/authApi';
   
   const response = await authApi.register({
     email: 'user@example.com',
     password: 'Password123',
     nickname: 'User'
   });
   
   // Token automatically stored in localStorage
   // apiClient.setToken() called automatically
   ```

2. **Protected Routes**:
   - Token хранится в `localStorage` как `gkeys_auth_token`
   - Автоматически добавляется ко всем запросам через `apiClient`
   - Backend валидирует token через `authenticate` middleware

3. **Token Refresh**:
   ```typescript
   await authApi.refreshToken(refreshToken);
   ```

### База данных

#### Основные модели

- **User**: Пользовательские аккаунты с аутентификацией
- **Game**: Игровые продукты с G2A интеграцией
- **Order**: Заказы с external order ID для G2A webhooks
- **Transaction**: Платежные транзакции
- **CartItem**: Товары в корзине
- **Wishlist**: Избранное пользователей
- **Category, Genre, Platform, Tag**: Метаданные игр
- **Session**: Сессии для guest пользователей

#### Связи

- User → Orders (один ко многим)
- User → CartItems (один ко многим)
- User → Wishlist (один ко многим)
- Game → CartItems (один ко многим)
- Game → Wishlist (один ко многим)
- Game → Categories, Genres, Platforms, Tags (многие ко многим)

---

## Troubleshooting

### Database Connection Error

**Проблема**: Не удается подключиться к базе данных PostgreSQL.

**Симптомы**:
- Ошибка: `Can't reach database server`
- Ошибка: `Connection refused`
- Ошибка: `Authentication failed`

**Решения**:

1. **Проверьте, запущен ли PostgreSQL**:
   ```bash
   # Docker
   docker ps | grep postgres
   
   # Локальный PostgreSQL
   pg_isready -h localhost -p 5432
   ```

2. **Проверьте DATABASE_URL**:
   ```bash
   # Убедитесь, что формат правильный
   echo $DATABASE_URL
   # Должен быть: postgresql://user:password@host:port/database
   ```

3. **Проверьте доступность базы данных**:
   ```bash
   psql $DATABASE_URL -c "SELECT 1;"
   ```

4. **Проверьте firewall/сетевые настройки**:
   - Убедитесь, что порт 5432 открыт
   - Проверьте, что хост доступен

5. **Для production (Vercel)**:
   - Убедитесь, что DATABASE_URL установлен в Environment Variables
   - Проверьте, что база данных разрешает подключения с IP адресов Vercel
   - Включите SSL: `?sslmode=require`

---

### Redis Connection Error

**Проблема**: Не удается подключиться к Redis.

**Симптомы**:
- Ошибка: `Redis connection failed`
- Ошибка: `ECONNREFUSED`
- Предупреждения в логах о недоступности Redis

**Решения**:

1. **Проверьте, запущен ли Redis**:
   ```bash
   # Локальный Redis
   redis-cli ping
   # Должен вернуть: PONG
   ```

2. **Проверьте REDIS_URL**:
   ```bash
   echo $REDIS_URL
   # Должен быть: redis://[password@]host:port[/database]
   ```

3. **Redis опционален**:
   - Приложение будет работать без Redis, но некоторые функции могут быть недоступны:
     - Кеширование будет отключено
     - Idempotency для webhooks может не работать
     - OAuth2 токены G2A не будут кешироваться

4. **Для production**:
   - Убедитесь, что Redis URL правильный
   - Проверьте доступность Redis с IP адресов Vercel
   - Убедитесь, что пароль правильный

---

### JWT Authentication Error

**Проблема**: Ошибки аутентификации, токены не работают.

**Симптомы**:
- Ошибка: `Invalid token`
- Ошибка: `Token expired`
- Ошибка: `Unauthorized`

**Решения**:

1. **Проверьте JWT_SECRET и JWT_REFRESH_SECRET**:
   ```bash
   # Убедитесь, что они установлены
   echo $JWT_SECRET
   echo $JWT_REFRESH_SECRET
   
   # Убедитесь, что они разные
   # Убедитесь, что они минимум 32 символа
   ```

2. **Проверьте время жизни токенов**:
   ```bash
   # Проверьте JWT_EXPIRES_IN и JWT_REFRESH_EXPIRES_IN
   echo $JWT_EXPIRES_IN
   echo $JWT_REFRESH_EXPIRES_IN
   ```

3. **Очистите localStorage** (frontend):
   ```javascript
   // В консоли браузера
   localStorage.removeItem('gkeys_auth_token');
   localStorage.removeItem('gkeys_refresh_token');
   ```

4. **Проверьте формат токена**:
   - Токен должен быть в формате: `Bearer <token>`
   - Проверьте, что токен передается в заголовке `Authorization`

---

### G2A API Error

**Проблема**: Ошибки при работе с G2A API.

**Симптомы**:
- Ошибка: `401 Unauthorized`
- Ошибка: `404 Not Found`
- Ошибка: `429 Too Many Requests`

**Решения**:

1. **Проверьте G2A credentials**:
   ```bash
   echo $G2A_API_KEY
   echo $G2A_API_HASH
   echo $G2A_API_URL
   echo $G2A_ENV
   ```

2. **Проверьте окружение**:
   - Убедитесь, что `G2A_ENV=sandbox` для тестирования
   - Используйте sandbox credentials для sandbox API
   - Используйте production credentials для production API

3. **Ошибка 401 Unauthorized**:
   - Проверьте, что API Key и API Hash правильные
   - Убедитесь, что они из одной пары (созданы вместе)
   - Для production: проверьте IP whitelist в G2A Seller Panel

4. **Ошибка 404 Not Found**:
   - Проверьте, что G2A_API_URL правильный
   - Для Import API: `https://api.g2a.com/integration-api/v1`
   - Для Export API (sandbox): `https://sandboxapi.g2a.com/v1`
   - Для Export API (production): `https://api.g2a.com/v1`

5. **Ошибка 429 Too Many Requests**:
   - G2A API имеет лимит 600 запросов в минуту
   - Добавьте задержки между запросами
   - Используйте кеширование для часто запрашиваемых данных

6. **OAuth2 Token 404**:
   - Убедитесь, что используется правильный endpoint: `GET /integration-api/v1/token`
   - Проверьте hash-based аутентификацию для получения токена
   - Убедитесь, что Import API активирован в G2A Seller Panel

---

### Frontend API Connection Error

**Проблема**: Frontend не может подключиться к backend API.

**Симптомы**:
- Ошибка: `Failed to fetch`
- Ошибка: `Network error`
- Ошибка: `CORS error`

**Решения**:

1. **Проверьте VITE_API_BASE_URL**:
   ```bash
   echo $VITE_API_BASE_URL
   # Должен быть: https://your-domain.com/api или http://localhost:3001/api
   # Должен начинаться с протокола (https:// или http://)
   # Должен заканчиваться на /api
   ```

2. **Проверьте CORS настройки**:
   - Убедитесь, что `FRONTEND_URL` установлен в backend
   - Проверьте, что frontend URL добавлен в CORS whitelist

3. **Проверьте, запущен ли backend**:
   ```bash
   curl http://localhost:3001/health
   # Должен вернуть: {"status":"ok",...}
   ```

4. **Для production (Vercel)**:
   - Убедитесь, что `VITE_API_BASE_URL` установлен правильно
   - Проверьте, что URL соответствует реальному деплою
   - Убедитесь, что используется `https://` для production

---

### Build Error

**Проблема**: Ошибки при сборке проекта.

**Симптомы**:
- Ошибка: `Module not found`
- Ошибка: `Type error`
- Ошибка: `Build failed`

**Решения**:

1. **Очистите кеш и переустановите зависимости**:
   ```bash
   # Frontend
   rm -rf node_modules package-lock.json
   npm install
   
   # Backend
   cd backend
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **Проверьте версии Node.js**:
   ```bash
   node --version
   # Должна быть >= 20.0.0
   ```

3. **Проверьте TypeScript ошибки**:
   ```bash
   # Frontend
   npm run build
   
   # Backend
   cd backend
   npm run build
   ```

4. **Для Vercel build**:
   - Проверьте логи сборки в Vercel Dashboard
   - Убедитесь, что все environment variables установлены
   - Проверьте, что `npm run vercel-build` работает локально

---

### Migration Error

**Проблема**: Ошибки при применении миграций базы данных.

**Симптомы**:
- Ошибка: `Migration failed`
- Ошибка: `Schema drift detected`
- Ошибка: `Database is not empty`

**Решения**:

1. **Проверьте подключение к базе данных**:
   ```bash
   cd backend
   npm run db:check
   ```

2. **Примените миграции вручную**:
   ```bash
   cd backend
   npm run prisma:migrate:deploy
   ```

3. **Сбросите базу данных (только для development!)**:
   ```bash
   cd backend
   npx prisma migrate reset
   # WARNING: Это удалит все данные!
   ```

4. **Проверьте схему Prisma**:
   ```bash
   cd backend
   npx prisma validate
   ```

5. **Для production**:
   - Никогда не используйте `prisma migrate reset` в production
   - Используйте `prisma migrate deploy` для применения миграций
   - Создайте backup базы данных перед применением миграций

---

### Cache Invalidation Issue

**Проблема**: Кеш не обновляется после изменений.

**Симптомы**:
- Старые данные отображаются на frontend
- Изменения не видны сразу после обновления

**Решения**:

1. **Проверьте Redis подключение**:
   ```bash
   redis-cli ping
   ```

2. **Очистите кеш вручную**:
   ```bash
   redis-cli
   > KEYS home:*
   > DEL home:*
   > KEYS game:*
   > DEL game:*
   > KEYS catalog:*
   > DEL catalog:*
   ```

3. **Проверьте инвалидацию кеша в коде**:
   - Убедитесь, что `invalidateCache()` вызывается после изменений
   - Проверьте логи - должны быть сообщения об инвалидации

4. **Для production**:
   - Убедитесь, что Redis доступен
   - Проверьте, что инвалидация кеша работает корректно
   - Рассмотрите использование TTL для автоматической инвалидации

---

### Session/Cart Migration Issue

**Проблема**: Товары из guest корзины не мигрируют после логина.

**Симптомы**:
- Корзина пустая после логина
- Товары из guest сессии не появляются в user корзине

**Решения**:

1. **Проверьте sessionId cookie**:
   - Убедитесь, что cookie `sessionId` установлен
   - Проверьте, что cookie передается в запросах

2. **Проверьте миграцию вручную**:
   ```bash
   curl -X POST http://localhost:3001/api/cart/migrate \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -H "Cookie: sessionId=YOUR_SESSION_ID"
   ```

3. **Проверьте логи**:
   - Должны быть сообщения о миграции корзины
   - Проверьте, что товары существуют в базе данных

4. **Проверьте автоматическую миграцию**:
   - Миграция должна происходить автоматически при логине
   - Проверьте `CartContext` и `WishlistContext` на frontend

---

### Admin Panel Access Issue

**Проблема**: Не удается получить доступ к админ-панели.

**Симптомы**:
- Ошибка: `403 Forbidden`
- Ошибка: `Unauthorized`
- Админ-панель не загружается

**Решения**:

1. **Проверьте, что пользователь является администратором**:
   ```sql
   -- В базе данных
   SELECT id, email, role FROM "User" WHERE email = 'your-email@example.com';
   -- role должен быть 'ADMIN'
   ```

2. **Обновите роль пользователя**:
   ```sql
   UPDATE "User" SET role = 'ADMIN' WHERE email = 'your-email@example.com';
   ```

3. **Проверьте JWT токен**:
   - Убедитесь, что токен валиден
   - Проверьте, что токен содержит правильную роль

4. **Проверьте middleware авторизации**:
   - Убедитесь, что `authenticate` middleware работает
   - Проверьте, что `requireAdmin` middleware проверяет роль

---

### Performance Issues

**Проблема**: Медленная работа приложения.

**Симптомы**:
- Долгая загрузка страниц
- Медленные API запросы
- Высокая нагрузка на базу данных

**Решения**:

1. **Используйте Redis для кеширования**:
   - Убедитесь, что Redis настроен
   - Проверьте, что кеширование работает

2. **Оптимизируйте запросы к базе данных**:
   - Используйте индексы для часто запрашиваемых полей
   - Избегайте N+1 запросов
   - Используйте `select` для получения только нужных полей

3. **Используйте пагинацию**:
   - Не загружайте все данные сразу
   - Используйте `page` и `limit` параметры

4. **Оптимизируйте frontend**:
   - Используйте code splitting
   - Используйте lazy loading для компонентов
   - Оптимизируйте изображения

5. **Мониторинг**:
   - Используйте логи для отслеживания медленных запросов
   - Проверьте метрики в Vercel Dashboard

---

## FAQ (Часто Задаваемые Вопросы)

### Общие вопросы

**Q: Как изменить порт backend сервера?**

A: Установите переменную окружения `PORT`:
```bash
PORT=3002 npm run dev
```

**Q: Как сбросить базу данных?**

A: Для development:
```bash
cd backend
npx prisma migrate reset
```
⚠️ **WARNING**: Это удалит все данные!

**Q: Как добавить нового администратора?**

A: Обновите роль пользователя в базе данных:
```sql
UPDATE "User" SET role = 'ADMIN' WHERE email = 'user@example.com';
```

**Q: Как проверить, что все работает?**

A: Запустите интеграционные тесты:
```bash
cd backend
npm run test:integration
```

### G2A Integration

**Q: Как переключиться с sandbox на production?**

A: Обновите environment variables:
- `G2A_ENV=live`
- `G2A_API_URL=https://api.g2a.com/integration-api/v1`
- Используйте production credentials

**Q: Почему G2A API возвращает 401?**

A: Проверьте:
- Правильность API Key и API Hash
- Что они из одной пары
- IP whitelist (для production)

**Q: Как проверить G2A синхронизацию?**

A: Используйте админ-панель:
- Перейдите в `/admin/g2a-sync`
- Нажмите "Start Sync"
- Проверьте прогресс синхронизации

### Deployment

**Q: Как обновить environment variables в Vercel?**

A: 
1. Перейдите в Vercel Dashboard → Project → Settings → Environment Variables
2. Добавьте или обновите переменную
3. Нажмите "Redeploy" для применения изменений

**Q: Как проверить логи в production?**

A: 
1. Перейдите в Vercel Dashboard → Project → Deployments
2. Выберите deployment
3. Нажмите "View Function Logs"

**Q: Как откатить deployment?**

A: 
1. Перейдите в Vercel Dashboard → Project → Deployments
2. Выберите предыдущий deployment
3. Нажмите "Promote to Production"

---

## Полезные ссылки

### Документация

- [React Documentation](https://react.dev/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [Prisma Documentation](https://www.prisma.io/docs/)
- [Express.js Documentation](https://expressjs.com/)
- [Vercel Documentation](https://vercel.com/docs)

### G2A API

- [G2A Integration API Documentation](https://www.g2a.com/integration-api/documentation/)
- [G2A Developers API Documentation](https://www.g2a.com/integration-api/documentation/export/)
- [G2A Seller Panel](https://www.g2a.com/cooperation/api-integration/)

### Инструменты

- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Redis Documentation](https://redis.io/docs/)
- [shadcn/ui Components](https://ui.shadcn.com/)

---

## Поддержка

### Получение помощи

Если вы столкнулись с проблемой, которая не описана в этом руководстве:

1. **Проверьте логи**:
   - Frontend: Browser DevTools → Console
   - Backend: Vercel Dashboard → Function Logs

2. **Проверьте Issues**:
   - Поищите похожие проблемы в репозитории

3. **Создайте Issue**:
   - Опишите проблему подробно
   - Приложите логи и скриншоты
   - Укажите шаги для воспроизведения

### Контакты

- **GitHub Repository**: [ссылка на репозиторий]
- **Email Support**: [email для поддержки]

---

## Changelog

### Версия 1.0.0

**Дата**: 2024-12-23

**Изменения**:
- ✅ Полная интеграция с G2A API (Import и Export)
- ✅ Поддержка OAuth2 токенов для Import API
- ✅ Кеширование в Redis
- ✅ Админ-панель с управлением G2A полями
- ✅ Миграция корзины и избранного для guest пользователей
- ✅ Полная документация

---

**Последнее обновление**: 2024-12-23

**Версия документации**: 1.0.0