# Contributing to GKEYS Store

Спасибо за интерес к проекту GKEYS Store! Это руководство поможет вам начать работу над проектом.

## 🚀 Getting Started

### 1. Fork и Clone

```bash
# Fork репозиторий на GitHub, затем:
git clone https://github.com/your-username/gkeys2.git
cd gkeys2
```

### 2. Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd backend
npm install
cd ..
```

### 3. Настройка окружения

Создайте файлы `.env`:

**Frontend** (`.env`):
```bash
VITE_API_BASE_URL=http://localhost:3001/api
```

**Backend** (`backend/.env`):
```bash
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gkeys_store
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/gkeys_store
JWT_SECRET=your-dev-secret-key-minimum-32-characters
JWT_REFRESH_SECRET=your-dev-refresh-secret-different-key
FRONTEND_URL=http://localhost:5173
NODE_ENV=development
```

См. [ENVIRONMENT_VARIABLES.md](ENVIRONMENT_VARIABLES.md) для полного списка переменных.

### 4. Настройка базы данных

```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed  # Опционально: заполнить тестовыми данными
```

### 5. Запуск в режиме разработки

**Terminal 1 - Frontend:**
```bash
npm run dev
```

**Terminal 2 - Backend:**
```bash
cd backend
npm run dev
```

Frontend: http://localhost:5173  
Backend API: http://localhost:3001

## 📝 Development Workflow

### Структура проекта

```
gkeys2/
├── src/                    # Frontend source code
│   ├── components/        # React components
│   ├── pages/            # Application pages
│   ├── services/         # API services
│   ├── hooks/            # Custom React hooks
│   └── types/            # TypeScript types
├── backend/              # Backend application
│   ├── src/
│   │   ├── routes/      # API routes
│   │   ├── services/    # Business logic
│   │   ├── controllers/ # Controllers
│   │   └── middleware/  # Express middleware
│   └── prisma/          # Database schema
├── docs/                # Component documentation
└── specs/              # Feature specifications
```

### Создание новой функции

1. **Создайте ветку:**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Разработайте функцию:**
   - Следуйте code style (см. ниже)
   - Пишите тесты для новой функциональности
   - Обновляйте документацию при необходимости

3. **Проверьте код:**
   ```bash
   # Frontend lint
   npm run lint
   
   # Backend lint
   cd backend
   npm run lint
   ```

4. **Создайте Pull Request:**
   - Опишите изменения
   - Укажите связанные issues (если есть)
   - Убедитесь, что все проверки пройдены

## 🎨 Code Style

### TypeScript

- Используйте строгую типизацию
- Избегайте `any` - используйте конкретные типы
- Используйте интерфейсы для объектов
- Используйте `type` для union types и aliases

**Пример:**
```typescript
// ✅ Хорошо
interface User {
  id: string;
  email: string;
}

type UserRole = 'admin' | 'user' | 'guest';

// ❌ Плохо
const user: any = { id: '1', email: 'test@example.com' };
```

### React Components

- Используйте функциональные компоненты с hooks
- Используйте TypeScript для пропсов
- Разбивайте большие компоненты на меньшие
- Используйте `React.memo` для оптимизации при необходимости

**Пример:**
```typescript
// ✅ Хорошо
interface GameCardProps {
  game: Game;
  onSelect: (id: string) => void;
}

export const GameCard: React.FC<GameCardProps> = ({ game, onSelect }) => {
  // Component logic
};
```

### Naming Conventions

- **Components**: PascalCase (`GameCard.tsx`)
- **Functions/Variables**: camelCase (`getUserData`)
- **Constants**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Files**: 
  - Components: PascalCase
  - Utilities: camelCase
  - Types: camelCase with `.types.ts` suffix

### File Organization

```
src/
├── components/
│   ├── ui/              # Reusable UI components
│   └── feature/         # Feature-specific components
├── pages/               # Page components
├── services/            # API services
├── hooks/               # Custom hooks
└── types/               # TypeScript types
```

## 🧪 Testing

### Frontend Testing

```bash
# Запустить тесты (если настроены)
npm run test
```

### Backend Testing

```bash
cd backend

# Тест endpoints
npm run test:endpoints

# Тест G2A интеграции
npm run test:g2a

# Проверка базы данных
npm run db:check
```

### Ручное тестирование

1. Проверьте функциональность в браузере
2. Проверьте консоль на ошибки
3. Проверьте Network tab для API запросов
4. Протестируйте на разных размерах экрана

## 📚 Documentation

### Обновление документации

При добавлении новой функции:

1. **Обновите README.md** если добавляете новую функциональность
2. **Добавьте комментарии** к сложной логике
3. **Обновите типы** в `types/` директории
4. **Создайте/обновите** документацию компонентов в `docs/`

### Комментарии в коде

```typescript
// ✅ Хорошо: объясняет "почему", а не "что"
// Используем debounce для уменьшения количества API запросов
const debouncedSearch = debounce(handleSearch, 300);

// ❌ Плохо: очевидный комментарий
// Увеличиваем счетчик
counter++;
```

## 🔄 Pull Request Process

### Перед созданием PR

- [ ] Код соответствует style guide
- [ ] Все тесты проходят
- [ ] Линтер не выдает ошибок
- [ ] Документация обновлена
- [ ] Коммиты логично организованы

### Описание PR

Включите:
- **Что изменилось** - краткое описание
- **Почему** - обоснование изменений
- **Как протестировать** - шаги для проверки
- **Скриншоты** (если UI изменения)

**Пример:**
```markdown
## Описание
Добавлена функция фильтрации игр по жанру

## Изменения
- Добавлен компонент GenreFilter
- Обновлен API endpoint для фильтрации
- Добавлены тесты

## Как протестировать
1. Откройте страницу каталога
2. Используйте фильтр по жанру
3. Убедитесь, что игры фильтруются корректно
```

## 🐛 Reporting Bugs

При сообщении о баге, укажите:

1. **Описание проблемы** - что происходит
2. **Шаги для воспроизведения**
3. **Ожидаемое поведение**
4. **Фактическое поведение**
5. **Окружение:**
   - OS и версия
   - Браузер и версия
   - Версия Node.js
6. **Скриншоты/логи** (если применимо)

## 💡 Feature Requests

При предложении новой функции:

1. Опишите проблему, которую она решает
2. Предложите решение
3. Объясните альтернативы, которые вы рассматривали
4. Укажите дополнительные контекст

## 📋 Code Review Guidelines

### Для авторов PR

- Будьте открыты к обратной связи
- Отвечайте на комментарии
- Вносите изменения по запросу ревьюеров

### Для ревьюеров

- Будьте конструктивны и вежливы
- Объясняйте свои замечания
- Предлагайте конкретные улучшения
- Хвалите хорошие решения

## 🔐 Security

Если вы нашли уязвимость безопасности:

1. **НЕ создавайте публичный issue**
2. Отправьте email на [security email] или создайте private security advisory
3. Опишите уязвимость детально
4. Предложите способ исправления (если возможно)

## 📞 Getting Help

- **Документация**: См. [README.md](README.md) и другие документы в корне проекта
- **Issues**: Проверьте существующие issues перед созданием нового
- **Discussions**: Используйте GitHub Discussions для вопросов

## ✅ Checklist для новых разработчиков

- [ ] Прочитал README.md
- [ ] Прочитал QUICK_START.md
- [ ] Настроил локальное окружение
- [ ] Запустил проект локально
- [ ] Понял структуру проекта
- [ ] Прочитал Code Style guide
- [ ] Создал первую ветку и сделал изменения

## 🎯 Development Priorities

При работе над проектом, учитывайте приоритеты:

1. **Безопасность** - всегда на первом месте
2. **Производительность** - оптимизация важна
3. **UX** - пользовательский опыт критичен
4. **Поддерживаемость** - код должен быть понятным
5. **Тестируемость** - код должен быть тестируемым

---

**Спасибо за ваш вклад в GKEYS Store!** 🚀
