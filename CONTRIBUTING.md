# Contributing Guide

Руководство по внесению вклада в проект GKEYS Store.

## 🚀 Быстрый старт

1. Прочитайте [QUICK_START.md](QUICK_START.md) для настройки окружения
2. Следуйте инструкциям в [PROJECT_HANDOVER.md](PROJECT_HANDOVER.md)
3. Ознакомьтесь с [DOCUMENTATION.md](DOCUMENTATION.md)

## 📋 Процесс разработки

### 1. Настройка окружения

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd gkeys2

# Установите зависимости
npm install
cd backend && npm install && cd ..

# Настройте переменные окружения
cd backend
npm run env:setup
# Отредактируйте .env файлы

# Настройте базу данных
npm run db:setup
```

### 2. Создание ветки

```bash
# Создайте ветку для вашей функции
git checkout -b feature/your-feature-name

# Или для исправления бага
git checkout -b fix/your-bug-fix
```

### 3. Разработка

- Следуйте существующим паттернам кода
- Пишите понятные комментарии
- Используйте TypeScript для типизации
- Следуйте стилю кода проекта (см. `.editorconfig`)

### 4. Тестирование

```bash
cd backend

# Запустите все тесты
npm test

# Запустите unit тесты
npm run test:unit

# Запустите integration тесты
npm run test:integration

# Проверьте покрытие
npm run test:coverage
```

### 5. Проверка кода

```bash
# Проверка линтером
npm run lint

# Форматирование кода
npm run format
```

### 6. Коммит изменений

```bash
# Добавьте изменения
git add .

# Создайте коммит с понятным сообщением
git commit -m "feat: add new feature description"
```

**Формат коммитов:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

### 7. Push и Pull Request

```bash
# Отправьте изменения
git push origin feature/your-feature-name

# Создайте Pull Request на GitHub/GitLab
```

## 📝 Стандарты кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any` типов
- Используйте интерфейсы для объектов
- Документируйте сложные функции

### React

- Используйте функциональные компоненты
- Используйте хуки вместо классов
- Разбивайте большие компоненты на меньшие
- Используйте TypeScript для пропсов

### Backend

- Следуйте RESTful принципам
- Используйте Prisma для работы с БД
- Обрабатывайте ошибки правильно
- Логируйте важные события

## 🗄️ Работа с базой данных

### Создание миграции

```bash
cd backend

# Создайте новую миграцию
npm run db:sync migrate migration_name

# Или используйте Prisma напрямую
npx prisma migrate dev --name migration_name
```

### Важные правила

- **Никогда не редактируйте существующие миграции**
- Создавайте новую миграцию для изменений
- Тестируйте миграции локально перед коммитом
- Проверяйте SQL перед применением

См. [backend/prisma/MIGRATIONS.md](backend/prisma/MIGRATIONS.md) для деталей.

## 🧪 Тестирование

### Unit тесты

Тесты для отдельных функций и компонентов.

```typescript
// Пример unit теста
import { describe, it, expect } from 'vitest';

describe('MyFunction', () => {
  it('should work correctly', () => {
    expect(myFunction()).toBe(expected);
  });
});
```

### Integration тесты

Тесты для взаимодействия компонентов.

```typescript
// Пример integration теста
import { describe, it, expect } from 'vitest';

describe('API Integration', () => {
  it('should handle requests correctly', async () => {
    const response = await fetch('/api/endpoint');
    expect(response.status).toBe(200);
  });
});
```

## 📚 Документация

### Обновление документации

- Обновляйте документацию при изменении API
- Добавляйте примеры использования
- Обновляйте CHANGELOG.md для значимых изменений

### Стиль документации

- Используйте Markdown
- Добавляйте примеры кода
- Объясняйте "почему", а не только "что"

## 🐛 Сообщение о багах

При сообщении о баге укажите:

1. Описание проблемы
2. Шаги для воспроизведения
3. Ожидаемое поведение
4. Фактическое поведение
5. Скриншоты (если применимо)
6. Версия Node.js, ОС и т.д.

## 💡 Предложения

При предложении новой функции:

1. Опишите проблему, которую она решает
2. Предложите решение
3. Объясните альтернативы
4. Покажите примеры использования

## ✅ Чеклист перед PR

- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлены новые тесты (если нужно)
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен (если нужно)
- [ ] Линтер не выдает ошибок
- [ ] Код отформатирован
- [ ] Коммиты имеют понятные сообщения

## 📞 Вопросы?

Если у вас есть вопросы:

1. Проверьте [DOCUMENTATION.md](DOCUMENTATION.md)
2. Проверьте [QUICK_START.md](QUICK_START.md)
3. Создайте issue на GitHub/GitLab
4. Свяжитесь с командой разработчиков

---

Спасибо за ваш вклад! 🎉
